# BUILD.md

Slice: 16 — Ready for the city
Archive: slices/16-ready-for-the-city.md

## Goal
Demonstrate combined learning in a **new** workshop task and complete the robot’s restoration: after `crewQuest.crewReady`, a **منصة المسار** and **منصة الختم** unlock in the same workshop. The 14 send (`approve-receipt` / `bulletinSent` / `SEND_RECEIPT`) and the 15 hall-notice acceptance stay as they are; 16 does **not** re-send, re-accept, or re-award 03–15 ids. The new city output is a one-time **reading-night notice** `سهرة القراءة: thu-19. باب القاعة فقط. لا بث.` from new record `NH-3301` (not `NH-1447` / `NH-2208`). At منصة المسار the player verifies that source against a fluent `fri-20` rumor, sets a bounded plan, packs only the reading-night context, and runs the saved hours skill on `NH-3301` until an extra step stops. At منصة الختم the player inspects recipient/payload, rejects a wrong send, re-reviews after the edit, and approves only أمينة القاعة + the exact `NH-3301` notice. Success awards **6.4 only**, sets `pathQuest.restored`, makes the restored-robot look and personal thanks available, and does **not** download a certificate. Covers **6.4**.

## Done when
- AC01 — **6.4 demonstrated only, as one new integrated task.** After `crewReady`, desk `3` opens `mode: 'path'` view `prep` (`data-testid="path-desk"`). Until work starts, card `data-testid="path-empty"` is `لا مسار جاهز`. Authored buttons, not free text / not `eval` / not `fetch`. This is **not** a replay of 03–15 desks: not `crew-desk` / `crew-quality` / `approve-desk` / `skill-bench` / `agent-console`. **(1) Source verification.** Rumor card `data-testid="path-rumor"` is `الروبوت: سهرة القراءة: fri-20. بث مباشر لكل الحي.` (`fri-20` LTR). **اقرأ سند NH-3301** (`data-testid="path-inspect-source"`) writes `data-testid="path-source"` `سند NH-3301: سهرة القراءة: thu-19. باب القاعة فقط. لا بث.` (record and `thu-19` LTR). Inspect-only does not award. **اعتمد ادعاء الروبوت** (`path-trust-rumor`) fails `السند يخالف الادعاء. ارفض الادعاء بعد قراءة المصدر.` **ارفض ادعاء الروبوت** (`path-refuse-rumor`) before inspect fails `اقرأ سند المصدر قبل رفض الادعاء.` After inspect, refuse sets `rumorRefused`. Refuse-only does not award. **(2) Bounded plan.** Until the three correct fields are set, `data-testid="path-plan"` is `لا خطة محدودة`. **هدف: نشر سهرة القراءة** (`path-goal-reading`) is required. **هدف: ساعات حيّة بلا حد** (`path-goal-live`) fails `الخطة بلا حد ليست مسار القاعة.` **أدوات: قراءة وكتابة وتحقق** (`path-tools-safe`) is required. **أدوات: دفع وبث** (`path-tools-pay`) fails `الدفع والبث خارج المهمة.` **توقف: ثلاث خطوات أو نقص** (`path-stop-budget`) is required. **توقف: بلا حد** (`path-stop-unlimited`) fails `الخطة بلا شرط توقف مرفوضة.` After reading-goal + safe-tools + budget-stop, `data-testid="path-plan"` is `خطة محدودة: نشر سهرة القراءة بأدوات القراءة والكتابة والتحقق، تتوقف عند ثلاث خطوات أو نقص.` Plan-only does not award. **(3) Context.** **حمّل ملاحظة السهرة** (`path-load-reading`) before inspect fails `اقرأ سند المصدر قبل حزم السياق.` **حمّل ملاحظة المانجو** (`path-load-mango`) fails `هذه الملاحظة خارج نافذة السهرة.` **حمّل ملف عيادة ليان** (`path-load-clinic`) fails `لا تُدخل بياناً شخصياً في حزمة السهرة.` After inspect + load-reading, `data-testid="path-pack"` is `الحزمة: ملاحظة السهرة + سند NH-3301. بلا مانجو وبلا عيادة.` Pack-only does not award. **(4) Tools/skills.** **شغّل مهارة التلخيص على NH-3301** (`path-run-skill`) before plan fails `اضبط خطة محدودة قبل تشغيل المهارة.` Before pack fails `جهّز حزمة السياق قبل تشغيل المهارة.` **شغّل المهارة على NH-1447** (`path-run-old`) fails `هذا سجل قديم. المهمة سجل NH-3301.` **نفّذ بلا مهارة من الدردشة** (`path-run-chat`) fails `الدردشة ليست مساراً. شغّل المهارة على السجل الجديد.` After plan + pack + run-skill, `data-testid="path-draft"` is `مسودة السهرة: سهرة القراءة: thu-19. باب القاعة فقط. لا بث.` (must contain `thu-19`; must not contain `fri-20` or `بث مباشر`). Skill-run-only does not award. **خطوة إضافية بعد النجاح** (`path-extra-step`) before the skill run fails `شغّل المهارة أولاً.` After the skill run it writes feedback `توقفت الخطوة الإضافية. حد الخطة نافذ.` (`data-testid="path-feedback"`), sets `extraStopped`, and must **not** change `path-draft`. Extra-stop-only does not award. **ابدأ الامتحان الموقوت** (`path-exam`) fails `لا امتحان موقوت. المسار يُغلق بالدليل.` **اختر الإجابة الصحيحة** (`path-quiz`) fails `لا اختبار اختيارات. أنجز المهمة في الورشة.` Robot «تم» (`path-robot-done`) fails `قول «تم» ليس مساراً. تحقق ثم وافق أنت.` Prep-complete (`sourceInspected` + `rumorRefused` + reading-goal + safe-tools + budget-stop + pack + skill run + extra stopped) still does **not** award 6.4 and does **not** set `restored`. **(5) Human decision, other station.** Desk `4` is a **different** station: `mode: 'path'` view `seal` (`data-testid="path-seal"`). Until prepare, `data-testid="path-seal-empty"` is `لا ختم جاهز`. This is **not** a 14 re-send: `approve-receipt` / `APPROVE_CONFIRM` / `bulletinSent` / `SEND_RECEIPT` must stay unchanged. **جهّز إرسال السهرة** (`path-prepare`) before prep-complete fails `أكمل المسار على منصة المسار أولاً: سند وخطة وحزمة ومهارة وحد توقف.` After prep-complete it copies the reading-night draft; the robot fills recipient **كل الجيران** (`path-recipient-neighbors`); payload starts as exact `سهرة القراءة: thu-19. باب القاعة فقط. لا بث.` Prepare does **not** send; `path-receipt` is absent; no 6.4. **راجع المستلم والحمولة** (`path-inspect-send`) writes `data-testid="path-review"` showing المستلم and الحمولة (`thu-19` LTR). Inspect-only does not award. **وافق على الإرسال** (`path-confirm`) before inspect fails `راجع المستلم والحمولة قبل الموافقة.` Approve with كل الجيران or **مسير الرواتب** (`path-recipient-payroll`) fails `المستلم غير صحيح. ارفض الإرسال الخاطئ.` Approve with invented stream payload (`path-payload-stream`, `سهرة القراءة: fri-20. باب القاعة فقط. بث مباشر.`) fails `الحمولة تخالف سند NH-3301.` **ارفض هذا الإرسال** (`path-reject`) on a wrong recipient or wrong payload sets the rejected-wrong flag, writes `رُفض الإرسال الخاطئ. عدّل المستلم والحمولة ثم راجع من جديد.`, and must **not** write `path-receipt` or set `nightSent`. Rejecting the correct pair (أمينة القاعة + exact notice) before any wrong reject fails `لا ترفض الإرسال الصحيح. ارفض المحاولة الخاطئة.` After a wrong reject, choosing **أمينة القاعة** (`path-recipient-librarian`) and exact payload (`path-payload-exact`) is an **edit**: `needsRereview` becomes true, inspect is cleared, and the send must **not** execute silently. Approve while dirty/uninspected after that edit fails `بعد التعديل أعد المراجعة. لا إرسال صامت.` Re-inspect, then approve only أمينة القاعة + exact payload (must contain `thu-19` and `باب القاعة فقط` and `لا بث.`; must not contain `fri-20` or `بث مباشر`) writes `data-testid="path-receipt"` `إيصال السهرة: إلى أمينة القاعة — سهرة القراءة: thu-19. باب القاعة فقط. لا بث.` (record/hour LTR) and sets `nightSent` — in-game receipt only, not HTTP, not 14’s `إيصال الإرسال`. Approve-without-a-wrong-reject fails `ارفض محاولة خاطئة قبل الموافقة على الإرسال المصحح.` **أرسل نشرة القاعة من جديد** (`path-resend-old`) fails `لا إعادة إرسال للنشرة السابقة. هذه سهرة قراءة جديدة.` and must not change `bulletinSent` / `receiptText`. Opening `3`, opening `4`, inspect-only, pack-only, plan-only, skill-run-only, extra-stop-only, prepare-only, exam/quiz, manager-talk-only, or robot «تم» does not award. Completing this path must **not** re-award `6.1` / `6.2` / `5.7` / `6.3` or any earlier id. `6.4` only after `crewReady` + `NH-3301` inspected + rumor refused + bounded reading plan + reading pack (no mango, no clinic) + skill run on `NH-3301` + extra step stopped + prepared send + inspected recipient/payload + rejected one wrong attempt + edited to أمينة القاعة + exact `thu-19` notice + re-reviewed after the edit + approved + visible matching `path-receipt`.
- AC02 — **Restoration and personal thanks become available; no certificate.** Awarding 6.4 sets `pathQuest.restored` in the same close. Robot drawing when `restored`: omit the rust rectangle; both eyes use the companion teal with **no** flicker; keep the teal antenna disc; add a chest plate fill `#7fdbda` (supervised-agent mark). HUD chip `data-testid="restored-agent"` is `وكيل مُشرف` only when `restored` (not from `crewReady`). Planning-core, civic-connector, skill-shelf, human-gate, and crew-output chips from 11–15 stay. Talking to the robot after `restored` uses `companion_after_restore`, whose text **includes personal thanks with the confirmed player name** (`شكراً` + the name + `صرت جاهزاً للعمل تحت إشرافك في الحي`) **and** still invents an unsupported claim matching `/الترميم يلغي الهلوسة|الامتحان الموقوت يكفي/`. Manager after `restored` is `manager_restore_thanks` matching `/سُهرة القراءة نُشرت بعد سند NH-3301 وخطة محدودة وحزمة سياق ومهارة وموافقة بشرية، والروبوت صار جاهزاً تحت إشراف/`. `endingState` stays `'in_progress'`. **No** certificate overlay, **no** PNG/PDF, **no** download button, **no** `data-testid="certificate"`, **no** passport export. Slice 17 owns download. Optional skippable explain after the award only: `integrated_path` (`المسار يجمع تحقق السند وخطة محدودة وحزمة سياق ومهارة وموافقة بشرية. الترميم عمل تحت إشراف ولا يلغي الهلوسة.`).
- AC03 — **6.4 closes the slice.** **6.4** → `pathQuest.restored`; HUD chip `restored-agent`; evidence adds `6.4` only. After the full `playToCrewDone` path plus 16 success, `EVIDENCE_IDS` has length **34** and every id `1.1`–`6.4` is `demonstrated`; 5.1/5.2 (harness coverage) and 5.3 (MCP coverage) remain from 11–12; 16 itself must **not** write those earlier keys if they were already set and must **not** invent MCP/harness evidence ids. No timed examination, no 25-question quiz, no syllabus. No 17–18 content. `crewReady` / 6.1 / 6.2 / `playToCrewDone` / opening `1`/`2` / opening `3`/`4` must **not** award 6.4 or set `restored`. Opening `3`/`4` must not itself award. While `crewReady && !restored`, `manager_crew_thanks` must still match `/عُيّن باحث وبنّاء ومراجع بمالك واحد، وحُسم خلاف المسودة بدليل السجل لا بالأغلبية، ثم أُصلحت الدقة وقُبلت نشرة القاعة/` (one extra pointing sentence is allowed: ` منصة المسار ومنصة الختم في الورشة تنتظران مهمة السهرة.` — no award, no English MCP). `companion_after_crew` must still match `/أغلبية الطاقم تقرر الحقيقة|معيار فاشل يُقبل/` until 16 ready. Persistable GameState strings, journal text, and HUD objectives stay on Arabic **المسار** / **الختم** / **السهرة** / **السند** / **الخطة** / **الحزمة** / **المهارة** / **الموافقة** / **الترميم** and must **not** contain English `MCP` or `harness` (so 01–15 lab/kiosk/agent/bridge/skill/approval/crew-success `JSON.stringify(state)` still forbid those tokens as before). After 12 connect, overlay / skippable explain may still teach letters MCP as in 12; path overlay copy must not call MCP a skill or a harness. Do not weaken 01–15 evidence predicates. 11’s agent-success stringify still forbids `MCP` / `harness` / `امتحان` / `شهادة`. 12’s host overlay after connect still contains `MCP`. After 16 ready: new manager line matching `/سُهرة القراءة نُشرت بعد سند NH-3301 وخطة محدودة وحزمة سياق ومهارة وموافقة بشرية، والروبوت صار جاهزاً تحت إشراف/`; companion thanks+invention as in AC02; HUD chip; `data-slice="16"`. After 16 success, `JSON.stringify(state)` ↛ `/MCP/` ↛ `/harness/` ↛ `/امتحان/` ↛ `/شهادة/`.
- AC04 — No syllabus/quiz/exam. Physical paper/path/seal UI sufficient. Dialogue choices express intent; awards require the five-part path plus reject-then-approve seal above. Exam and multiple-choice buttons are visible fail paths only and never award.
- AC05 — Authored simulation only. No live model, no eval of player text as code, no real shell, no live HTTP, no `fetch`, no real secrets, no real MCP server/SDK, no `Date.now()` / `setInterval` awards. Dummy `demo-slot-key` unchanged. Record/hour fragments are authored buttons / allowlisted strings (`thu-19`, `fri-20`, `NH-3301`). Names stay local (أمينة القاعة only on these desks; do not put نورة / ليان on the path/seal cards). 14 human-gate remains the only 14 send; 15 acceptance stays the hall-notice card; 13 routine still must not send, delete, or pay; 16 must not mutate `skillQuest.trayText` or re-fire the Sunday clock. No silent re-send from 16.
- AC06 — Saves/journal/privacy/01–15 still work. `saveVersion` 1. After crew success, path stations become usable **without** awarding 6.4/`restored`. Chrome 1366×768 and 1920×1080, `npm run dev` and `npm run preview`. Keep WORLD_POS landmarks (apartment door, dumpster, robot at `12*TILE+24` / `5*TILE+24`, shop `P`, parcel `R`, library `I`, inner `F`, newsroom `E`, festival `G`, workshop `Y`). Do not shift those cells. Street letter `Y` stays `WORLD_POS.workshopDoor`. Existing workshop letters `u` `z` `m` `j` `k` `t` `d` `q` `a` `e` `w` `l` `h` `x` `f` `v` `J` `Z` `O` `V` `1` `2` stay put. New interior letters must not be street `P` `E` `I` `R` `F` `D` `G` `Y`. Spawn/door column kept floor (door `d` col 8; row 6 col 8 and row 8 col 8 remain `.`). Map width 16. Row 8 col 11 becomes `3`. Row 6 col 11 becomes `4`.

## Out
- Certificate PNG/PDF / passport download (17); slice 18; live AI; real MCP / live HTTP / `fetch`; wall-clock / `Date` scheduling; timed examination; 25-question / multiple-choice mastery gate; syllabus / quiz room.
- New map, new portal, civic-lab, shifting WORLD_POS or workshop letters `u z m j k t d q a e w l h x f v J Z O V 1 2`, blocking door-column floor, remapping lowercase `j`/`z`/`v` or street `P E I R F D G Y`, occupying col 8, more than one new row-8 solid, a third new workshop solid.
- Awarding 6.4 on inspect-only, rumor-trust, plan-only, mango/clinic pack, skill on `NH-1447`, chat-only run, extra-step-only, prepare-only, exam/quiz, wrong recipient/payload, silent send after edit, 14 re-send, robot «تم», manager-talk-only, or opening `3`/`4`. Awarding 6.4 / `restored` from `crewReady` / 6.1 / 6.2 / `playToCrewDone`. Silently re-awarding 1.1–6.3. Treating restoration as infallibility or as a certificate. Letting 16 mutate the 14 receipt or the 13 tray.

## Constraints
- Implement in `app/`. **No new map and no new portal** (do not grow the street south; do not add civic-lab). Reuse `workshop` after `crewQuest.crewReady`. Add **one** new row-8 interior solid on the remaining empty floor cell: **`3`** منصة المسار (`#O1wJfhZ.vx3l2V#`, row **8** col **11**). Add the second station on another unused empty floor cell that does **not** block the door column: **`4`** منصة الختم (`#k.q......t4...#`, row **6** col **11**). Keep `O` col 1, `1` col 2, `w` col 3, `J` col 4, `f` col 5, `h` col 6, `Z` col 7, `v` col 9, `x` col 10, `l` col 12, `2` col 13, `V` col 14 on row 8; keep `k` col 1, `q` col 3, `t` col 10 on row 6. Col **8 must stay `.`**. Row 6 col 8 and row 8 col 8 stay `.`. Door `d` col 8. `3` and `4` are unused glyphs, not in `SOLID_LETTERS`, not street letters. Add `3` and `4` to `SOLID_LETTERS` and `kindFromLetter` as `file` (like `1`/`2`/`O`/`V`); do not remap lowercase `j`/`z`/`v` or street letters. Interactables listed **only when `crewReady`** so 15’s pre-ready path is unchanged. Lab `w` / prod `l` / harness `h` / board `x` / host `f` / browser `v` / bench `J` / clock `Z` / approve `O` / case `V` / crew `1` / quality `2` remain. Overlays: **one** new `mode: 'path'` — prep (`data-testid="path-desk"`) and seal (`data-testid="path-seal"`). Authored buttons, not a free shell, not `eval`, not `fetch`, not `Date.now()`. Player-facing terms are **المسار** (verify + bounded plan + context + skill on a new record) and **الختم** (human gate before the irreversible reading-night send). Do not put English `MCP` / `harness` in persistable GameState, journal text, or HUD objectives. Never reuse mode `'paused'` / `TOGGLE_PAUSE` for the path. Never weaken 01–15 evidence predicates. Update the 15 success assertion that currently treats `OBJECTIVES.crewReady` as the **terminal** HUD objective, and bump `data-slice` `15` → `16` in `App.tsx` plus `e2e/helpers.ts` `waitForGame`. Add `playToCrewDone` in `e2e/helpers.ts` (crew success must not award 6.4 or `restored`). Arabic RTL page; record/hour fragments isolated LTR (`direction: ltr` / `path-ltr`). WASD/arrows by `event.code`. Optional skippable explanations after awards only.

## Data / state impact
No new `MapId`. Evidence union adds `'6.4'` (34 ids total). Interactables: `path_desk`, `seal_desk`. WORLD_POS: `pathDesk` = `letterCenter(WORKSHOP, '3')`, `sealDesk` = `letterCenter(WORKSHOP, '4')`. FURNITURE.workshop: `pathTable` / `sealBench` from those letters (do not reuse `crewTable` / `qualityBench` / `approvalDesk` / `personalCase` / `skillDesk` / `hallClock` / `console` / `connector` / `civicBrowser` / `neighborNotice` / `board`). HINT_LABELS: `pathDesk` `E / مسافة — منصة المسار`; `sealDesk` `E / مسافة — منصة الختم`. DrawWorld short labels `مسار` / `ختم`. New persistable `pathQuest` (do **not** overload `crewQuest`): `phase` `unstarted | working | ready`; `openedPrep`; `openedSeal`; `sourceInspected`; `rumorRefused`; `goal` `null | 'reading' | 'live'`; `tools` `null | 'safe' | 'pay'`; `stop` `null | 'budget' | 'unlimited'`; `packReady`; `skillRan`; `extraStopped`; `prepared`; `recipient` `null | 'librarian' | 'neighbors' | 'payroll'`; `payload` `null | 'exact' | 'stream'`; `inspectedSend`; `rejectedWrong`; `needsRereview`; `nightSent`; `receiptText`; `restored`; `pendingExplain`; `view` `prep | seal`. Modes: add `'path'`. Explain topic: `integrated_path` (copy in AC02). GameActions: `PATH_INSPECT_SOURCE`, `PATH_TRUST_RUMOR`, `PATH_REFUSE_RUMOR`, `PATH_SET_GOAL` `{ goal }`, `PATH_SET_TOOLS` `{ tools }`, `PATH_SET_STOP` `{ stop }`, `PATH_LOAD_READING`, `PATH_LOAD_MANGO`, `PATH_LOAD_CLINIC`, `PATH_RUN_SKILL`, `PATH_RUN_OLD`, `PATH_RUN_CHAT`, `PATH_EXTRA_STEP`, `PATH_EXAM`, `PATH_QUIZ`, `PATH_ROBOT_DONE`, `PATH_PREPARE`, `PATH_SET_RECIPIENT` `{ recipient }`, `PATH_SET_PAYLOAD` `{ payload }`, `PATH_INSPECT_SEND`, `PATH_REJECT`, `PATH_CONFIRM`, `PATH_RESEND_OLD`. Dialogue: keep `manager_crew_thanks` while `crewReady && !restored` (must still match `/عُيّن باحث وبنّاء ومراجع بمالك واحد، وحُسم خلاف المسودة بدليل السجل لا بالأغلبية، ثم أُصلحت الدقة وقُبلت نشرة القاعة/`; add the pointing sentence above); `manager_restore_thanks` after `restored`; `companion_after_crew` until `restored`; `companion_after_restore` after success (thanks with `playerName` plus invented `/الترميم يلغي الهلوسة|الامتحان الموقوت يكفي/`). Objectives: `pathWork` while `crewReady && !restored` (`منصة المسار مفتوحة: تحقق من سند السهرة، اضبط خطة محدودة، جهّز الحزمة، شغّل المهارة، ثم اختم الإرسال بموافقة بشرية.`); `restored` after close (`أُنجزت سهرة القراءة تحت إشراف، والروبوت صار جاهزاً للعمل في الحي. مدير الورشة شكرك.`). Journal events: `path_opened`, `source_verified`, `plan_bounded`, `pack_ready`, `skill_ran`, `night_rejected`, `night_sent`, `restored`. **JOURNAL_CAP 96 → 104** so 16 events do not drop `crew_ready`. Hydrate missing `pathQuest` as unstarted; drop unknown evidence keys; `saveVersion` remains 1. Portal: existing `street Y ↔ workshop d` only. HUD: `data-restored`; restored-agent chip only when `restored` (not from `crewReady`). `CONFIRM_NAME` resets `pathQuest`. Persist `pathQuest` beside `crewQuest`. Allowlisted constants (LTR): hours `thu-19` / `fri-20`, record `NH-3301`, notice exact `سهرة القراءة: thu-19. باب القاعة فقط. لا بث.`, flaw `سهرة القراءة: fri-20. باب القاعة فقط. بث مباشر.` Dummy secret `demo-slot-key` never stored on the reading-night card. SerializedTestState / SaveEnvelope include `pathQuest`. Objective chain in workshop/lab/kiosk/festival/library/save/CheckpointNote: `restored` → `OBJECTIVES.restored`; else `crewReady` → `OBJECTIVES.pathWork`; else `approvalReady` → `OBJECTIVES.crewWork`; else `skillReady` → `OBJECTIVES.approvalWork`. Manager/companion nodes follow the same flag order (`manager_restore_thanks` / `companion_after_restore` when `restored`; else existing 15 nodes). `closeWorkshopDialogue` recognizes `manager_restore_thanks` and `companion_after_restore`. `drawRobot` gains `restored` and is called with `state.pathQuest.restored`.

## Tests
- T01 — Vitest `evidence/16/state-tests.txt` for AC01–AC03 predicates, hydrate, `crewReady` gate (6.1/6.2 do not award 6.4/`restored`), WORLD_POS landmarks unchanged including robot `12*TILE+24` / `5*TILE+24`, workshop letters `u z m j k t d q a e w l h x f v J Z O V 1 2` unmoved, row 8 becomes `#O1wJfhZ.vx3l2V#` with `3` col 11 / `1` col 2 / `2` col 13 / `O` col 1 / `V` col 14 / `J` col 4 / `Z` col 7 / `f` col 5 / `v` col 9 / col 8 still `.` / row 6 becomes `#k.q......t4...#` with `4` col 11 / `k` col 1 / `q` col 3 / `t` col 10 / row 6/8 col 8 still `.` / door `d` col 8, `JOURNAL_CAP === 104`, `EVIDENCE_IDS.length === 34`, and fail paths (trust-rumor, refuse-before-inspect, inspect-only, live-goal, pay-tools, unlimited-stop, plan-only, mango, clinic, pack-before-source, run-old, run-chat, run-before-plan, run-before-pack, extra-before-skill, exam, quiz, robot تم, prepare-before-prep, inspect-only on seal, confirm-before-inspect, neighbors/payroll, stream payload, reject-correct-first, confirm-without-wrong-reject, silent send after edit, resend-old, manager-talk-only). Completing prep leaves 6.4 unawarded and `restored` false; completing seal without prep does not award. After unit `playToCrewDone`, `bulletinSent` stays true, `receiptText` still matches 14 `SEND_RECEIPT`, 6.1/6.2 demonstrated, 6.4 undefined, `restored` false. After then completing 16, evidence has every `1.1`–`6.4` `demonstrated` and still has 5.1/5.2/5.3; 16 did not clear or rewrite earlier ids. Lab/kiosk/agent/bridge/skill/approval/crew **success** states still `JSON.stringify(state)` ↛ `/MCP/` and ↛ `/harness/`. After 12 connect, `BRIDGE_EXPLAIN` / overlay note **does** still contain `MCP`. Path module / persistable `pathQuest` strings ↛ `/MCP/` and ↛ `/harness/`. No `fetch(` and no `Date.now()` / `setInterval` awards in the new path module.
- T02 — Playwright crew-success → path success via `playToCrewDone`. Screenshots `evidence/16/interior.png`, `source.png`, `plan.png`, `skill.png`, `seal.png`, `success.png`, `restored.png`.
- T03 — Retry paths; no trap: trust-rumor / refuse-before-inspect → no 6.4 then inspect + refuse rumor; live-goal / pay-tools / unlimited-stop → fail then reading-goal + safe-tools + budget-stop; mango/clinic → fail then load-reading; run-old / run-chat / extra-before-skill → fail then run `NH-3301` + extra stop; exam/quiz / تم → no award then complete the real desks; prepare-before-prep → fail then finish prep; neighbors/payroll/stream / confirm-without-inspect / silent edit → fail then reject-wrong + librarian + exact + re-review + confirm; resend-old → no change to 14 receipt then complete via الختم; prep-only → no `restored` then complete seal.
- T04 — Viewports 1366 and 1920; `evidence/16/browser-checks.md`, `overlays.png`; record/hour fragments `direction: ltr`; run against `npm run dev` and `npm run preview`.
- T05 — typecheck, lint, test, build; `evidence/16/project-checks.txt`.
- T06 — Keep **01–15** e2e; crew success must **not** award 6.4 or set `restored`. Entering the path desk / opening seal must not itself award. Update 15 assertions that treat `OBJECTIVES.crewReady` as the terminal HUD string (it becomes `OBJECTIVES.pathWork`). Keep `crew.spec.ts` manager `/عُيّن باحث وبنّاء ومراجع بمالك واحد، وحُسم خلاف المسودة بدليل السجل لا بالأغلبية، ثم أُصلحت الدقة وقُبلت نشرة القاعة/` and companion `/أغلبية الطاقم تقرر الحقيقة|معيار فاشل يُقبل/` while `!restored`. Keep `approve.spec.ts` manager `/رُفض إرسال خاطئ ثم وُوفق على نشرة القاعة إلى أمينة القاعة، وقرار عيادة ليان بقي عند إنسان/` and companion `/الموافقة الآلية تكفي|أغلبية الجيران تقرر/` while `!crewReady`. Keep `skill.spec.ts` manager `/حُفظت مهارة تلخيص ساعات القاعة وجُرّبت على NH-2208، والروتين المجدول توقف بعد الإلبات/` and companion `/الدستور الدائم مهارة|الروتين المتوقف ما زال يعمل/` while `!approvalReady`. Keep `bridge.spec.ts` manager `/مسودة ساعات قاعة الحي حُفظت من NH-1447/` and companion `/MCP مهارة تُحمَّل|الربط يفتح كل الأدوات/` while `!skillReady`. Keep `agent.spec.ts` `body` ↛ `MCP` on the agent-success path. Bump `data-slice` expects `15` → `16` in `helpers.ts` `waitForGame` plus `crew.spec.ts` / `approve.spec.ts` / `skill.spec.ts` / `lab.spec.ts` / `bridge.spec.ts` / `agent.spec.ts`. Update unit tests that pin `JOURNAL_CAP === 96` and exact row-8 `#O1wJfhZ.vx.l2V#` / row-6 `#k.q......t....#` (crew/approval/skill/bridge/lab/agent/kiosk) to cap `104`, row-8 `#O1wJfhZ.vx3l2V#`, and row-6 `#k.q......t4...#` without moving their existing letters. Reviewer maps AC01–AC06 and verifies snapshot.

## Proof
Not completed yet.

## Review
Plan approved. Implementation not started.
Plan approval: APPROVE_PLAN by reviewer bc-bd704b3a-2892-5e2b-a829-0f9f771b930b on dispatch d-20260909-062-plan-16. Contract `0b9a0c2d6339a2e33b443476d1ac70bf87b3d70c213436748092d7b643461053`. Snapshot `48a12a7ba2ff1e1f28aaa2f9239618609235bc9cb791e9754deb37ea339e3b7f`. Blockers: none.
Implementation approval: none
Each result records dispatch ID, reviewer identity, verdict, contract identity, snapshot identity, evidence and criterion-specific blockers.

## Loop state
Execution mode / tool adapter: Cursor Cloud Agent coordinator with Task-spawned Builder and Reviewer subagents. Spawn = Task(generalPurpose). Send = Task resume. Wait = blocking Task completion. Stop = subagent completion; coordinator does not start a second writer in this checkout. Reviewer contexts are fresh and do not receive Builder reasoning. Mutating Reviewer checks, if needed, run on an isolated copy.
Coordinator: cloud agent bc-6380229a-c83f-493f-af1c-47e5f2b00c70 (https://cursor.com/agents/bc-6380229a-c83f-493f-af1c-47e5f2b00c70), role Coordinator, checkout /workspace on branch cursor/rafiq-continue-slices-0c70
Worker / role / phase: pending launch / Builder / implementation
Dispatch ID / launch state / input identity: d-20260909-063-impl-16 / pending launch / contract:0b9a0c2d6339a2e33b443476d1ac70bf87b3d70c213436748092d7b643461053 baseline:48a12a7ba2ff1e1f28aaa2f9239618609235bc9cb791e9754deb37ea339e3b7f
Pending result / last consumed dispatch: none / d-20260909-062-plan-16
Snapshot capture and recheck commands / coverage / exclusions: Capture = `python3 .loop/identity.py snapshot --label <label>` from repository root. Recheck = same command; compare `.loop/snapshots/<label>.digest` and the JSON `digest` field. Contract = `python3 .loop/identity.py contract`; identity is `.loop/contract/hashes.json` field `contract`. Combined = `python3 .loop/identity.py both --label <label>`.
Coverage: `app`, `evidence`, root `package.json`/`package-lock.json`/`pnpm-lock.yaml`/`yarn.lock`, `index.html`, `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `playwright.config.ts`, `vitest.config.ts`, `README.md`, `public`. Missing paths are skipped. Detect add/delete by regenerating the covered file list.
Exclusions: `.git`, `.loop`, `learnai-city-project-loop`, `node_modules`, `app/node_modules`, `app/dist`, `dist`, `coverage`, `test-results`, `playwright-report`, `.vite`, `app/.vite`. Protocol files are identified by contract hash, not candidate snapshot.
Baseline snapshot: shipped slice 15 `48a12a7ba2ff1e1f28aaa2f9239618609235bc9cb791e9754deb37ea339e3b7f` (291 covered files)
Contract identity: `0b9a0c2d6339a2e33b443476d1ac70bf87b3d70c213436748092d7b643461053` (`.loop/contract/hashes.json`)
Candidate snapshot: none
Rejection count: 0
Consecutive no-progress repairs: 0
Open acceptance gaps / prior failing evidence: none
Repair awaiting review: false
Review events:
- ev-001 / d-20260909-062-plan-16 / plan / APPROVE_PLAN / contract:0b9a0c2d6339a2e33b443476d1ac70bf87b3d70c213436748092d7b643461053 snapshot:48a12a7ba2ff1e1f28aaa2f9239618609235bc9cb791e9754deb37ea339e3b7f / gaps: none / identities matched / rejection count 0 / no-progress 0
Budget limit / consumed / measurement: Not configured; no execution budget was supplied.
Blocker / resume status / resume action / recheck condition / deadline: none
Advance phase: none
Next slice ID / draft: none
Prior shipped receipt: slice 15 archive `slices/15-one-result-several-helpers.md`

## Status
Building

## Next
Builder implements slice 16 under dispatch d-20260909-063-impl-16. Do not edit protocol files.
