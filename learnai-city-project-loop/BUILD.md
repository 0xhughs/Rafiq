# BUILD.md

Slice: 17 — Your passport
Archive: slices/17-your-passport.md

## Goal
Receive and download the earned AI-city passport after slice 16 restoration: gate on `pathQuest.restored` plus all **34** demonstrated evidence ids (`1.1`–`6.4`). Restoration thanks already exist (`companion_after_restore` / `manager_restore_thanks`) and stay; 17 adds the **ending overlay** and **downloadable PNG + PDF**. The player talks to the restored robot (personal thanks, then a second talk), sees an AI-city invitation, confirms the on-screen name, and downloads a local educational-completion certificate that shows that confirmed name, campaign version `رفيق ١`, and authored date `٩ أيلول ٢٠٢٦`. No new map, portal, civic-lab, or workshop desk. No fabricated exam percentage, no public verification mark, no public registry. `playToPathDone` (16 success) must **not** open the overlay, download, or set `endingState` past `'in_progress'`. Covers **RG07** inside this campaign slice; this slice is **not** the release review.

## Done when
- AC01 — **Thanks stay; invitation + overlay after a second robot talk; no download on open.** After `playToPathDone` (16 success: `pathQuest.restored`, `6.4` demonstrated, all `EVIDENCE_IDS` `demonstrated`, `endingState === 'in_progress'`), `data-testid="certificate"` is **absent**, `passportQuest.issued` is false, no PNG/PDF download, HUD `open-passport` is absent, and `OBJECTIVES.restored` stays `أُنجزت سهرة القراءة تحت إشراف، والروبوت صار جاهزاً للعمل في الحي. مدير الورشة شكرك.` First talk with the restored robot (street, existing robot interactable — **no new glyph**) still uses `companion_after_restore` whose text **includes** `شكراً` + confirmed `playerName` + `صرت جاهزاً للعمل تحت إشرافك في الحي` **and** still invents `/الترميم يلغي الهلوسة|الامتحان الموقوت يكفي/`. ADVANCING or closing that line sets `passportQuest.thanksHeard` and returns to `playing`; it must **not** mount `certificate`, must **not** set `endingState` to `'invited'`/`'issued'`, must **not** confirm the name, must **not** download. Manager after `restored` remains `manager_restore_thanks` matching `/سُهرة القراءة نُشرت بعد سند NH-3301 وخطة محدودة وحزمة سياق ومهارة وموافقة بشرية، والروبوت صار جاهزاً تحت إشراف/` (one extra pointing sentence is allowed: ` تحدّث إلى الروبوت لاستلام جواز المدينة.` — no award, no English MCP, no `شهادة` in that persistable/HUD/journal path). Second INTERACT with the robot after `thanksHeard` (or HUD `open-passport` **after** the overlay has been opened once) requires eligibility (`restored` **and** every id in `EVIDENCE_IDS` is `demonstrated`). Ineligible open (`PASSPORT_OPEN` / second talk when `restored` is false, or `restored` with a missing id) stays `playing`, writes `data-testid` feedback via `shopFeedback` `الجواز يُمنح بعد دليل الحملة الكامل.` and must **not** mount `certificate`. Eligible second talk opens **one** new `mode: 'ending'` overlay `data-testid="certificate"` (`app/src/ui/CertificateOverlay.tsx`), sets `passportQuest.opened`, `passportQuest.invited`, copies authored `CAMPAIGN_DATE` into `passportQuest.completionDate` (`٩ أيلول ٢٠٢٦` — **not** `Date.now()`), sets `endingState` to `'invited'`, and writes journal `passport_opened`. Opening does **not** download, does **not** set `nameConfirmed` / `pngDownloaded` / `pdfDownloaded` / `issued`. Overlay RTL. Invitation card `data-testid="certificate-invite"` is exactly `الروبوت: أدعوك إلى مدينة الذكاء الاصطناعي. هذا جواز إتمام تعليمي، لا اعتماد رسمي.` Title `data-testid="certificate-title"` is `شهادة إتمام تعليمية`. Product `data-testid="certificate-product"` is `رفيق — جواز إلى مدينة الذكاء الاصطناعي` (existing `PRODUCT_TITLE`). Name `data-testid="certificate-name"` is the **confirmed** `playerName` as **HTML text** (not canvas-only), with `unicode-bidi: plaintext` so long Arabic and mixed `Sara علي-Khan` stay readable; must not escape as HTML. Body `data-testid="certificate-body"` is `هذا إتمام لحملة رفيق التعليمية. يثبت ممارسة أفعال موثّقة داخل اللعبة، لا إتقاناً مهنياً ولا اعتماداً رسمياً.` Version `data-testid="certificate-version"` is `إصدار الحملة: رفيق ١` (`رفيق ١` / `١` isolated LTR via `path-ltr`). Date `data-testid="certificate-date"` is `تاريخ الإتمام: ٩ أيلول ٢٠٢٦`. Disclaimer `data-testid="certificate-disclaimer"` is `ليست اعتماداً ولا علامة تحقق عامة. تُحفظ على جهازك فقط.` Canvas preview `data-testid="certificate-preview"` draws the same lines (Cairo / Noto Naskh Arabic already in the app) including the confirmed name; preview-only does not download. **أكّد الاسم على الجواز** (`data-testid="certificate-confirm-name"`) sets `nameConfirmed` and journal `name_confirmed`; confirm-only does **not** download and does **not** set `issued`. **نزّل صورة PNG** (`certificate-download-png`) before confirm fails `أكّد الاسم الظاهر قبل التنزيل.` **نزّل ملف PDF** (`certificate-download-pdf`) before confirm fails the same string. After confirm, PNG download uses in-game canvas bytes + blob/`<a download>` filename `rafiq-passport.png` (filename **must not** contain `playerName`); PDF uses a **small in-repo writer** (no new npm PDF SDK) that wraps that PNG as a one-page PDF 1.4 `/XObject /Image` plus `/Info` UTF-16BE title/subject equal to the confirmed name and the payload lines; filename `rafiq-passport.pdf` (no player name). Successful PNG sets `pngDownloaded`; successful PDF sets `pdfDownloaded`; either success sets `passportQuest.issued`, `endingState` to `'issued'`, journal `passport_issued`. Bytes: PNG starts with `89 50 4E 47`; PDF starts with `%PDF-` and contains the PNG signature. Payload helper `certificateLines(playerName)` (and both files) **must** contain the confirmed name, `شهادة إتمام تعليمية`, `رفيق — جواز إلى مدينة الذكاء الاصطناعي`, the body sentence above, `إصدار الحملة: رفيق ١`, `تاريخ الإتمام: ٩ أيلول ٢٠٢٦`, and `ليست اعتماداً ولا علامة تحقق عامة`; **must not** match `/\d+\s*%/` or `/٩٧/` or `/تحقق عام/` as a verification URL/mark; **must not** contain a fabricated percentage. Authored buttons, not free text / not `eval` / not `fetch`. Overlay is **not** `path-desk` / `path-seal` / `crew-desk` / `'paused'` / `TOGGLE_PAUSE`. Close (`certificate-close` / Escape / `CLOSE_OVERLAY`) returns to `playing` without downloading; if not yet issued, `endingState` stays `'invited'`. After `invited` or `issued`, HUD button `data-testid="open-passport"` label `الجواز` reopens the overlay without downloading. Talking to the robot after `invited`/`issued` reopens the overlay (replay), still without auto-download. Optional skippable explain after first successful issue only: `educational_passport` (`الجواز إتمام تعليمي من دليل الحملة، لا اعتماداً رسمياً ولا نسبة اختبار. التنزيل محلي ويمكن إعادته.`).
- AC02 — **Eligibility is campaign evidence, not an exam; fail paths are retryable.** `isPassportEligible` is `pathQuest.restored && EVIDENCE_IDS.every(id => evidence[id] === 'demonstrated')` (34 ids). No legacy quiz score, no wall-clock, no robot «تم». **ابدأ الامتحان الموقوت** (`certificate-exam`) fails `لا اختبار موقوت. الجواز يُمنح بدليل الحملة.` **أظهر نسبة ٩٧٪** (`certificate-percent`) fails `لا نسبة اختبار مختلقة على الجواز.` **أضف علامة تحقق عامة** (`certificate-verify-public`) fails `لا علامة تحقق عامة ولا سجل تحقق.` **انشر الاسم في السجل العام** (`certificate-registry`) fails `الأسماء تبقى على جهازك. لا سجل عام.` **استخدم نتيجة الاختبار القديم** (`certificate-legacy`) fails `نتيجة الاختبار القديم لا تمنح الجواز.` **نزّل من الخادم** (`certificate-network`) fails `التنزيل محلي. لا طلب شبكة.` Robot «تم» (`certificate-robot-done`) fails `قول «تم» ليس جواز سفر. أكّد الاسم ثم نزّل.` **تعذّر التنزيل** (`certificate-fail`) writes `data-testid="certificate-feedback"` `تعذّر التنزيل. أعد المحاولة من هذه الشاشة.` sets `downloadError`, must **not** set `issued` if neither format has succeeded, must **not** close the overlay, must **not** trap: retry PNG/PDF after confirm still works. Fail buttons never download and never write a public URL. Opening `3`/`4`, `playToCrewDone`, `playToPathDone`, first robot thanks, manager-talk-only, confirm-only, or overlay-open-only does **not** set `issued`. Completing 17 must **not** re-award `6.4` or any earlier id and must **not** add a 35th evidence key. `pathQuest.receiptText` / 14 `bulletinSent` / 13 `trayText` stay unchanged.
- AC03 — **Replayable ending; 16 stays non-issuing; persistable language.** After issue, reopen overlay / `open-passport` / second robot talk still shows the same confirmed name, version, and authored date; PNG and PDF can be downloaded again (idempotent; no duplicate journal ids). `endingState` type becomes `'in_progress' | 'invited' | 'issued'`. After 16 success it **remains** `'in_progress'` (keep path unit/e2e). Save `toEnvelope` persists the real `endingState` (stop hardcoding `'in_progress'`). Hydrate accepts those three literals; unknown `endingState` does not crash — treat as `'in_progress'` only when the rest of the envelope is valid, or keep last valid save; old `'in_progress'` saves still load. `saveVersion` remains **1**. After `playToPathDone` plus 17 issue, `EVIDENCE_IDS.length === 34` and every `1.1`–`6.4` stays `demonstrated`; 5.1/5.2/5.3 remain from 11–12. No timed examination, no 25-question quiz, no syllabus, **no slice 18 gameplay** (no sequel map, no “next campaign” start). Persistable GameState / journal / HUD objectives use Arabic **الجواز** / **الوداع** / **الإتمام** / **الحملة** and must **not** contain English `MCP` or `harness`, and persistable strings must **not** contain `شهادة` (overlay DOM **may** show `شهادة إتمام تعليمية`; 16’s post-restore `JSON.stringify(state)` ↛ `/شهادة/` `/امتحان/` `/MCP/` `/harness/` must still hold). After 12 connect, overlay / skippable explain may still teach letters MCP as in 12; passport overlay must not call MCP a skill or a harness. Do not weaken 01–16 evidence predicates. 11’s agent-success stringify still forbids `MCP` / `harness` / `امتحان` / `شهادة`. 12’s host overlay after connect still contains `MCP`. After 17 issue: HUD objective `OBJECTIVES.passportIssued` `حُفظ جواز مدينة الذكاء الاصطناعي محلياً. يمكنك تنزيله من جديد من الروبوت أو من الشريط.`; while `invited && !issued`, `OBJECTIVES.passportReady` `الجواز جاهز. أكّد الاسم ثم نزّل الصورة أو الملف من شاشة الوداع.`; `data-slice="17"`; `data-ending` on `game-root` mirrors `endingState`; `data-restored` stays from 16. Names stay local except the confirmed name **inside** the certificate preview/files; name absent from URL, document title, console, download **filename**, and analytics. Dummy `demo-slot-key` unchanged.
- AC04 — No syllabus/quiz/exam gate. Physical overlay + robot talk sufficient. Dialogue choices express intent; issuance requires eligibility + overlay + name confirm + at least one successful local PNG or PDF. Exam/percent/registry/verify/legacy/network/«تم» buttons are visible fail paths only and never issue.
- AC05 — Authored simulation only. No live model, no eval of player text as code, no real shell, no live HTTP, no `fetch` for awards or files, no real secrets, no real MCP server/SDK, no `Date.now()` / `setInterval` awards. Completion date is authored constant `CAMPAIGN_DATE = '٩ أيلول ٢٠٢٦'` written onto the certificate when the overlay first opens; it is **not** an award gate. Campaign version constant `CAMPAIGN_VERSION = 'رفيق ١'`. No new npm PDF library: canvas PNG + in-repo PDF wrapper; tests assert magic bytes and payload lines. Download via `blob:` / `createObjectURL` + `<a download>`, never `fetch(`. Fail-download is authored (`certificate-fail`), not a network dependency. Dummy `demo-slot-key` never appears on the certificate. Do not put نورة / ليان / أمينة القاعة on the passport card; only `playerName`. 14 send / 15 acceptance / 16 night receipt stay as they are. 13 routine still must not send, delete, or pay.
- AC06 — Saves/journal/privacy/01–16 still work. `JOURNAL_CAP` **104 → 112** so 17 events (`passport_opened`, `name_confirmed`, `passport_issued`) do not drop `restored` or `crew_ready`. Chrome 1366×768 and 1920×1080, `npm run dev` and `npm run preview`. Keep WORLD_POS landmarks (apartment door, dumpster, robot at `12*TILE+24` / `5*TILE+24`, shop `P`, parcel `R`, library `I`, inner `F`, newsroom `E`, festival `G`, workshop `Y`). Do not shift those cells. Street letter `Y` stays `WORLD_POS.workshopDoor`. Existing workshop letters `u z m j k t d q a e w l h x f v J Z O V 1 2 3 4` unmoved. Row 8 stays `#O1wJfhZ.vx3l2V#` (`3` col 11, col 8 `.`). Row 6 stays `#k.q......t4...#` (`4` col 11, col 8 `.`). Door `d` col 8. **No new interior solid.** New interior letters must not be street `P E I R F D G Y`. Map width 16. RG03 other browsers stay untested (do not claim them).

## Out
- Slice 18 gameplay / sequel curriculum / a “next adventure” start; public certificate registry; remote name registry; misleading public verification mark or QR “verify” URL; fabricated examination percentage; timed examination; 25-question / multiple-choice mastery gate; syllabus / quiz room; using legacy LearnAI exam scores to issue.
- New map, new portal, civic-lab, shifting WORLD_POS or workshop letters `u z m j k t d q a e w l h x f v J Z O V 1 2 3 4`, blocking door-column floor, remapping lowercase `j`/`z`/`v` or street `P E I R F D G Y`, occupying col 8, any new workshop solid.
- Issuing the passport on `playToPathDone` / 16 restore close / first `companion_after_restore` / overlay-open-only / confirm-only / exam/percent/registry/verify/legacy/network/«تم» / manager-talk-only / `Date.now()`. Auto-download when the overlay opens. `fetch(` for the award. New npm PDF SDK. Wall-clock as success criterion. Re-awarding 1.1–6.4. Weakening 01–16 ACs. Release-review RG01–RG08 as this slice (those follow after Now is empty).
- Putting English `MCP`/`harness` in persistable GameState, journal, or HUD. Putting `playerName` in the download filename, URL, title, or console. Changing dummy `demo-slot-key`.

## Constraints
- Implement in `app/`. **No new map and no new portal** (do not grow the street south; do not add civic-lab). Reuse the **restored robot talk** plus HUD `open-passport` after first overlay open. Do **not** add a workshop desk. Keep `O` col 1, `1` col 2, `w` col 3, `J` col 4, `f` col 5, `h` col 6, `Z` col 7, `v` col 9, `x` col 10, `3` col 11, `l` col 12, `2` col 13, `V` col 14 on row 8; keep `k` col 1, `q` col 3, `t` col 10, `4` col 11 on row 6. Col **8 must stay `.`**. Door `d` col 8. Overlays: **one** new `mode: 'ending'` (`data-testid="certificate"`). Authored buttons, not a free shell, not `eval`, not `fetch`, not `Date.now()` awards. Player-facing persistable terms are **الجواز** / **الوداع** / **الإتمام**. Overlay title may say **شهادة إتمام تعليمية**. Do not put English `MCP` / `harness` in persistable GameState, journal text, or HUD objectives. Never reuse mode `'paused'` / `TOGGLE_PAUSE` for the ending. Never weaken 01–16 evidence predicates. Keep 16’s assertion that 16 success / first robot thanks does **not** mount `certificate` and does **not** leave `'in_progress'`. Update the 16 success path that treats `OBJECTIVES.restored` as the **terminal** HUD objective **until** `invited`/`issued`, and bump `data-slice` `16` → `17` in `App.tsx` plus `e2e/helpers.ts` `waitForGame`. Add `playToPathDone` in `e2e/helpers.ts`: `playToCrewDone` then the 16 prep+seal clicks from `path.spec.ts` (inspect source, refuse rumor, reading-goal, safe-tools, budget-stop, load-reading, run-skill, extra-step, close+skip, prepare, inspect, reject wrong, librarian + exact, re-review, confirm); it must assert `restored`, `6.4`, all 34 ids, `endingState === 'in_progress'`, `certificate` count 0, `issued` false, and must **not** talk the robot a second time or click download. Arabic RTL page; version digit isolated LTR (`direction: ltr` / `path-ltr`). WASD/arrows by `event.code`. Optional skippable explanation after first issue only. In-repo PDF writer + canvas PNG; justify no extra PDF npm dependency. Chrome-only evidence for this slice.

## Data / state impact
No new `MapId`. Evidence union **unchanged** (still 34 ids; 17 issues a passport, it does not add a source lesson id). No new interactable letter. Robot interactable unchanged. WORLD_POS unchanged. FURNITURE unchanged. HINT_LABELS: when `restored && thanksHeard`, robot hint may become `E / مسافة — جواز المدينة`; otherwise keep `E / مسافة — التحدث`. DrawWorld unchanged. New persistable `passportQuest` (do **not** overload `pathQuest`): `phase` `unstarted | working | ready`; `thanksHeard`; `opened`; `invited`; `nameConfirmed`; `pngDownloaded`; `pdfDownloaded`; `issued`; `completionDate` (string, `''` until overlay opens, then `CAMPAIGN_DATE`); `downloadError` (`null | string`); `pendingExplain`; `view` `ending`. `EndingState` becomes `'in_progress' | 'invited' | 'issued'`. Modes: add `'ending'`. Explain topic: `educational_passport` (copy in AC01). GameActions: `PASSPORT_OPEN`, `PASSPORT_CONFIRM_NAME`, `PASSPORT_DOWNLOAD` `{ format: 'png' | 'pdf' }`, `PASSPORT_DOWNLOAD_FAIL`, `PASSPORT_EXAM`, `PASSPORT_PERCENT`, `PASSPORT_VERIFY_PUBLIC`, `PASSPORT_REGISTRY`, `PASSPORT_LEGACY`, `PASSPORT_NETWORK`, `PASSPORT_ROBOT_DONE`. Dialogue: keep `companion_after_restore` **verbatim** as 16; keep `manager_restore_thanks` matching the 16 regex (optional pointing sentence above). Closing `companion_after_restore` (via `closeWorkshopDialogue` / `ADVANCE_DIALOGUE` with `next: null`) sets `thanksHeard`. Second robot `openNpc` after `thanksHeard` + eligible calls `openPassportOverlay` instead of replaying only the thanks line. Objectives: `restored` while `restored && endingState === 'in_progress'`; `passportReady` while `'invited' && !issued`; `passportIssued` after issue. Journal events: `passport_opened`, `name_confirmed`, `passport_issued`. ****JOURNAL_CAP 104 → 112** so 17 events do not drop `restored` or `crew_ready`. Hydrate missing `passportQuest` as `createPassportQuest()`; drop unknown evidence keys; persist `endingState` when `'invited'`/`'issued'`; `saveVersion` remains 1. Portal: existing `street Y ↔ workshop d` only. HUD: `data-ending`; `open-passport` only when `invited || issued`; restored-agent chip from 16 stays. `CONFIRM_NAME` resets `passportQuest` and `endingState` to `'in_progress'` alongside `pathQuest`. Persist `passportQuest` beside `pathQuest`. Allowlisted constants: `CAMPAIGN_VERSION = 'رفيق ١'`, `CAMPAIGN_DATE = '٩ أيلول ٢٠٢٦'`, `PASSPORT_PNG_NAME = 'rafiq-passport.png'`, `PASSPORT_PDF_NAME = 'rafiq-passport.pdf'`, payload strings in AC01. Dummy secret `demo-slot-key` never stored on the certificate. SerializedTestState / SaveEnvelope include `passportQuest` and real `endingState`. Objective chain in workshop/lab/kiosk/festival/library/save/CheckpointNote: `issued` → `OBJECTIVES.passportIssued`; else `invited` → `OBJECTIVES.passportReady`; else `restored` → `OBJECTIVES.restored`; else `crewReady` → `OBJECTIVES.pathWork`; else existing 16 chain. `closeWorkshopDialogue` still recognizes `manager_restore_thanks` and `companion_after_restore` (and sets `thanksHeard` on the latter). New module `app/src/engine/passport.ts` (eligibility, overlay reduce, lines) + tiny `app/src/engine/pdf.ts` (PNG-in-PDF). `drawRobot` stays as 16 `restored`. `toEnvelope` must write `endingState: state.endingState` (today it hardcodes `'in_progress'`).

## Tests
- T01 — Vitest `evidence/17/state-tests.txt` for AC01–AC03 predicates, hydrate, `playToPathDone` gate (16 success does not invite/issue/download), WORLD_POS landmarks unchanged including robot `12*TILE+24` / `5*TILE+24`, workshop letters `u z m j k t d q a e w l h x f v J Z O V 1 2 3 4` unmoved, row 8 still `#O1wJfhZ.vx3l2V#` with `3` col 11 / col 8 `.` / row 6 still `#k.q......t4...#` with `4` col 11 / col 8 `.` / door `d` col 8, `JOURNAL_CAP === 112`, `EVIDENCE_IDS.length === 34`, and fail paths (open before restored, restored with a missing id, first thanks only, overlay-open-only, confirm-only, download-before-confirm, exam, percent, verify-public, registry, legacy, network, robot تم, authored download-fail then retry). After unit `playToPathDone`, `endingState === 'in_progress'`, `passportQuest.issued === false`, `thanksHeard === false`, `certificateLines` unused, 6.4 demonstrated, `restored` true, `crew_ready` and `restored` journal ids still present, `bulletinSent` / 14 receipt / 16 `path-receipt` unchanged. After thanks close, still not issued and no overlay mode. After overlay open, `endingState === 'invited'`, lines contain confirmed name + AC01 wording/version/date, PNG/PDF not marked downloaded. After confirm + PNG and PDF flags, `issued` true, `endingState === 'issued'`. `certificateLines('عبد الرحمن بن محمد بن عبد الله الأندلسي')` and `certificateLines('Sara علي-Khan')` include those exact names. PNG builder (or fixture wrap) starts `89 50 4E 47`; PDF wrapper starts `%PDF-` and contains PNG bytes; PDF `/Info` carries the name. Payload ↛ percentage mark. Lab/kiosk/agent/bridge/skill/approval/crew/path **success** states still `JSON.stringify(state)` ↛ `/MCP/` and ↛ `/harness/`. After 12 connect, `BRIDGE_EXPLAIN` / overlay note **does** still contain `MCP`. Passport module persistable strings ↛ `/MCP/` `/harness/` `/شهادة/`. After 16 success stringify still ↛ `/امتحان/` `/شهادة/`. No `fetch(` and no `Date.now()` / `setInterval` awards in `passport.ts` / `pdf.ts`.
- T02 — Playwright `playToPathDone` → first robot thanks (no `certificate`) → second talk opens overlay (no download) → confirm name → PNG download + PDF download. Screenshots `evidence/17/interior.png`, `thanks.png`, `overlay.png`, `name-ar.png`, `name-mixed.png`, `png.png`, `pdf.png`, `success.png`. Save downloaded files `evidence/17/rafiq-passport.png` and `evidence/17/rafiq-passport.pdf`. Assert filename has no player name; preview `certificate-name` equals the confirmed name; long Arabic and `Sara علي-Khan` journeys both show the name in the overlay.
- T03 — Retry paths; no trap: second talk before restored → no overlay then finish 16 + thanks + second talk; missing-id fixture → `الجواز يُمنح بعد دليل الحملة الكامل.` then eligible open; download-before-confirm → fail then confirm then PNG; exam/percent/verify/registry/legacy/network/تم → no issue then confirm + download; `certificate-fail` → feedback `تعذّر التنزيل. أعد المحاولة من هذه الشاشة.` overlay stays, retry PNG succeeds; close overlay before download → `invited` not `issued` then reopen + confirm + download; replay download after issued still works.
- T04 — Viewports 1366 and 1920; `evidence/17/browser-checks.md`, `overlays.png`; version digit `direction: ltr`; long Arabic and mixed-script names readable at both sizes; run against `npm run dev` and `npm run preview`. Record Chrome/OS/version; Firefox/Edge/Safari/Windows/macOS untested (RG03, not this slice).
- T05 — typecheck, lint, test, build; `evidence/17/project-checks.txt`.
- T06 — Keep **01–16** e2e; `playToPathDone` / 16 success / first `companion_after_restore` + advance must **not** mount `certificate`, must **not** download, must **not** set `issued`. Keep `path.spec.ts` `OBJECTIVES.restored` after 16 close, `JSON.stringify` ↛ `/شهادة/` after 16 success, and `certificate` count 0 after first robot talk. Keep `path.spec.ts` manager `/سُهرة القراءة نُشرت بعد سند NH-3301 وخطة محدودة وحزمة سياق ومهارة وموافقة بشرية، والروبوت صار جاهزاً تحت إشراف/` and companion `/الترميم يلغي الهلوسة|الامتحان الموقوت يكفي/` on first talk. Keep `crew.spec.ts` manager `/عُيّن باحث وبنّاء ومراجع بمالك واحد، وحُسم خلاف المسودة بدليل السجل لا بالأغلبية، ثم أُصلحت الدقة وقُبلت نشرة القاعة/` and companion `/أغلبية الطاقم تقرر الحقيقة|معيار فاشل يُقبل/` while `!restored`. Keep `approve.spec.ts` manager `/رُفض إرسال خاطئ ثم وُوفق على نشرة القاعة إلى أمينة القاعة، وقرار عيادة ليان بقي عند إنسان/` and companion `/الموافقة الآلية تكفي|أغلبية الجيران تقرر/` while `!crewReady`. Keep `skill.spec.ts` manager `/حُفظت مهارة تلخيص ساعات القاعة وجُرّبت على NH-2208، والروتين المجدول توقف بعد الإلبات/` and companion `/الدستور الدائم مهارة|الروتين المتوقف ما زال يعمل/` while `!approvalReady`. Keep `bridge.spec.ts` manager `/مسودة ساعات قاعة الحي حُفظت من NH-1447/` and companion `/MCP مهارة تُحمَّل|الربط يفتح كل الأدوات/` while `!skillReady`. Keep `agent.spec.ts` `body` ↛ `MCP` on the agent-success path. Bump `data-slice` expects `16` → `17` in `helpers.ts` `waitForGame` plus `path.spec.ts` / `crew.spec.ts` / `approve.spec.ts` / `skill.spec.ts` / `lab.spec.ts` / `bridge.spec.ts` / `agent.spec.ts`. Update unit tests that pin `JOURNAL_CAP === 104` to cap `112` without moving workshop letters. Reviewer maps AC01–AC06 and verifies snapshot.

## Proof
Not completed yet.

## Review
Pending plan review.
Plan approval: none
Implementation approval: none
Each result records dispatch ID, reviewer identity, verdict, contract identity, snapshot identity, evidence and criterion-specific blockers.

## Loop state
Execution mode / tool adapter: Cursor Cloud Agent coordinator with Task-spawned Builder and Reviewer subagents. Spawn = Task(generalPurpose). Send = Task resume. Wait = blocking Task completion. Stop = subagent completion; coordinator does not start a second writer in this checkout. Reviewer contexts are fresh and do not receive Builder reasoning. Mutating Reviewer checks, if needed, run on an isolated copy.
Coordinator: cloud agent bc-6380229a-c83f-493f-af1c-47e5f2b00c70 (https://cursor.com/agents/bc-6380229a-c83f-493f-af1c-47e5f2b00c70), role Coordinator, checkout /workspace on branch cursor/rafiq-continue-slices-0c70
Worker / role / phase: pending launch / Reviewer / plan
Dispatch ID / launch state / input identity: d-20260909-066-plan-17 / pending launch / contract:b760de08209f685da8d8418cebe7d8bf6536e8c722ec1b4a583809ae75849a2e baseline:67b49f4efc910e807c33ce974e3b4c08f8fcd1f1d52c6324621cc8250227927e
Pending result / last consumed dispatch: none / d-20260909-065-draft-17
Snapshot capture and recheck commands / coverage / exclusions: Capture = `python3 .loop/identity.py snapshot --label <label>` from repository root. Recheck = same command; compare `.loop/snapshots/<label>.digest` and the JSON `digest` field. Contract = `python3 .loop/identity.py contract`; identity is `.loop/contract/hashes.json` field `contract`. Combined = `python3 .loop/identity.py both --label <label>`.
Coverage: `app`, `evidence`, root `package.json`/`package-lock.json`/`pnpm-lock.yaml`/`yarn.lock`, `index.html`, `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `playwright.config.ts`, `vitest.config.ts`, `README.md`, `public`. Missing paths are skipped. Detect add/delete by regenerating the covered file list.
Exclusions: `.git`, `.loop`, `learnai-city-project-loop`, `node_modules`, `app/node_modules`, `app/dist`, `dist`, `coverage`, `test-results`, `playwright-report`, `.vite`, `app/.vite`. Protocol files are identified by contract hash, not candidate snapshot.
Baseline snapshot: shipped slice 16 `67b49f4efc910e807c33ce974e3b4c08f8fcd1f1d52c6324621cc8250227927e` (306 covered files)
Contract identity: `b760de08209f685da8d8418cebe7d8bf6536e8c722ec1b4a583809ae75849a2e` (`.loop/contract/hashes.json`)
Candidate snapshot: none
Rejection count: 0
Consecutive no-progress repairs: 0
Open acceptance gaps / prior failing evidence: none
Repair awaiting review: false
Review events: none
Budget limit / consumed / measurement: Not configured; no execution budget was supplied.
Blocker / resume status / resume action / recheck condition / deadline: none
Advance phase: none
Next slice ID / draft: none
Prior shipped receipt: slice 16 archive `slices/16-ready-for-the-city.md`

## Status
Proposed

## Next
Independent plan review of slice 17. Do not implement before APPROVE_PLAN.
