# BUILD.md

Slice: R1 — Resume and leftover opening copy
Archive: slices/r1-resume-and-leftover-opening-copy.md

## Goal
Close the in-authority gaps from release review `d-20260909-069-release` without opening slice 18, other browsers, a beginner pilot, or publish/deploy. After a complete 01–17 campaign the opening checkpoint must no longer say remaining puzzles are still in development. Playwright must save/reload an **unfinished later-arc puzzle** (منصة المسار after `playToCrewDone`, not shop-enter-after-help and not neighbor postpone) and save/reload an **issued** ending, with no duplicate evidence, item, upgrade, journal id, or premature certificate. Existing corrupt/denied-storage and neighbor-postpone coverage in `save.spec.ts` stays; it is not this slice’s goal. Covers the in-authority part of **RG02** plus leftover opening copy. `data-slice` stays **`"17"`**. `JOURNAL_CAP` stays **112**. This slice is **not** the release review.

## Done when
- AC01 — **Leftover opening checkpoint copy is gone; leads stay; issued copy unchanged.** The default `CheckpointNote` text (visible on `data-testid="slice-checkpoint"` after `playToHelpAccepted` / `encounter === 'help_accepted'` and **before** `shopQuest.phase === 'helped'`) is exactly `رفيق أصبح رفيقك في الحي. البقالة عند الزاوية مفتوحة الآن، وبعدها واجهة المكتبة.` It **must not** match `/قيد التطوير/`, **must not** claim the store is still closed (`/سيُفتح|مغلق/`), and must still send the player to the corner store and library lead (contains `بقالة` and `مكتبة`). HUD `data-testid="hud-objective"` after help-accepted and before shop-helped stays `OBJECTIVES.cornerStore` `لنبدأ بالمتجر عند الزاوية: ادخل بقالة الزاوية، ثم سر إلى واجهة المكتبة.` (unchanged). After `issued` / `endingState === 'issued'`, `slice-checkpoint` and `hud-objective` are `OBJECTIVES.passportIssued` `حُفظ جواز مدينة الذكاء الاصطناعي محلياً. يمكنك تنزيله من جديد من الروبوت أو من الشريط.` While `invited && !issued`, both use `OBJECTIVES.passportReady` `الجواز جاهز. أكّد الاسم ثم نزّل الصورة أو الملف من شاشة الوداع.` After `restored` and before invite/issue, both use `OBJECTIVES.restored` `أُنجزت سهرة القراءة تحت إشراف، والروبوت صار جاهزاً للعمل في الحي. مدير الورشة شكرك.` Later checkpoint branches (shopHelped through `crewReady`) stay as they are. No new journal id. Do **not** claim Chrome/Edge Windows, Firefox Windows, or Safari macOS.
- AC02 — **Playwright: unfinished later-arc puzzle save/reload.** This is **منصة المسار**, not shop-enter and not neighbor postpone. `playToCrewDone` then `interactAt` workshop `WORLD_POS.pathDesk` opens `data-testid="path-desk"` (`mode: 'path'`, view `prep`). Until inspect, `data-testid="path-empty"` is `لا مسار جاهز`. Click **اقرأ سند NH-3301** (`path-inspect-source`) then **ارفض ادعاء الروبوت** (`path-refuse-rumor`) then **هدف: نشر سهرة القراءة** (`path-goal-reading`). Stop. Do **not** set tools/stop, do **not** `path-load-reading`, **not** `path-run-skill`, **not** `path-extra-step`, **not** seal desk, **not** award. Before reload: `path-source` is `سند NH-3301: سهرة القراءة: thu-19. باب القاعة فقط. لا بث.` (`NH-3301` / `thu-19` LTR via `path-ltr`); `path-empty` is absent; `path-plan` is still `لا خطة محدودة`; `pathQuest.sourceInspected === true`; `rumorRefused === true`; `goal === 'reading'`; `tools === null`; `packReady === false`; `skillRan === false`; `restored === false`; `evidence['6.4']` undefined; `endingState === 'in_progress'`; `passportQuest.issued === false`; `data-testid="certificate"` count 0; journal `source_verified` length **1** and `path_opened` length **1**. Screenshot `evidence/r1/unfinished.png`. Reload, `waitForGame` (still `data-slice="17"`). `name-overlay` count 0; `data-mode` is **`playing`** (do **not** persist overlay mode; slice 02 hydrate remains `playing`). Map is `workshop`. Re-`interactAt` `pathDesk`. Overlay `path-desk` is visible again with the same unfinished progress: `path-source` still the NH-3301 line, `path-empty` absent, `path-plan` still `لا خطة محدودة`, `path-goal-reading` selected (`primary`), `sourceInspected` / `rumorRefused` / `goal === 'reading'` restored, still no `6.4`, still `!restored`, still no `certificate`, still `endingState === 'in_progress'`. No duplicate `source_verified` or `path_opened`. No cloned inventory item, no second robot, no duplicate `6.1`/`6.2`. Screenshot `evidence/r1/resume-puzzle.png`. Then `completePathQuest` (idempotent remaining clicks) still awards **6.4 once**, sets `restored`, journal `restored` length **1**, `EVIDENCE_IDS.length === 34`, `certificate` still absent, `issued` still false. Do not use shop-enter-after-help or neighbor postpone as this AC; those already pass in `save.spec.ts`.
- AC03 — **Playwright: issued ending save/reload.** `playToPathDone` → first robot talk (`companion_after_restore`, includes `شكراً` + confirmed `playerName` + `صرت جاهزاً للعمل تحت إشرافك في الحي` and still invents `/الترميم يلغي الهلوسة|الامتحان الموقوت يكفي/`) → advance (no `certificate`, `endingState` still `'in_progress'`) → second talk opens `data-testid="certificate"` without downloading → **أكّد الاسم على الجواز** (`certificate-confirm-name`) → **نزّل صورة PNG** (`certificate-download-png`) **or** **نزّل ملف PDF** (`certificate-download-pdf`) so `passportQuest.issued === true` and `endingState === 'issued'` → close overlay / skip explain if open → `open-passport` label `الجواز` → reload → `waitForGame`. After reload: `name-overlay` count 0; `data-mode` is `playing`; `data-ending` is `issued`; `endingState === 'issued'`; `passportQuest.issued === true`; `nameConfirmed === true`; at least the downloaded format flag stays true; `data-slice="17"`; HUD and `slice-checkpoint` are `OBJECTIVES.passportIssued`; journal `passport_issued` length **1** (reopen must not append a second id); `EVIDENCE_IDS` still 34 all `demonstrated`; 6.4 not re-awarded. Overlay is **reopenable**: HUD `open-passport` or a further robot talk mounts `certificate` without auto-download. Confirmed name still HTML text on `certificate-name`. Replay PNG and/or PDF: suggested filenames still `rafiq-passport.png` / `rafiq-passport.pdf` (`PASSPORT_PNG_NAME` / `PASSPORT_PDF_NAME`); filenames **must not** contain `playerName`. Screenshot `evidence/r1/issued-reload.png`. Do not persist `mode: 'ending'` across reload.
- AC04 — No syllabus/quiz/exam gate. No new lecture, lesson carousel, or timed examination. Dialogue choices still express intent. R1 awards nothing new: it only resumes existing 03–17 predicates and replaces leftover copy. Path exam/quiz/«تم» buttons stay fail-only as in 16. Passport exam/percent/registry/verify/legacy/network/«تم» stay fail-only as in 17.
- AC05 — Authored simulation only. No live model, no `eval` of player text, no real shell, no live HTTP, no `fetch` for awards or files, no real secrets, no real MCP server/SDK, no `Date.now()` / `setInterval` awards. No new npm PDF SDK. Dummy `demo-slot-key` unchanged. Persistable GameState / journal / HUD still must **not** contain English `MCP` or `harness`, and persistable strings still must **not** contain `شهادة` (overlay DOM may still show `شهادة إتمام تعليمية`). After 12 connect, overlay / skippable explain may still teach letters MCP as in 12.
- AC06 — Saves/journal/privacy/01–17 still work. `saveVersion` remains **1**. `JOURNAL_CAP` remains **112** (no new journal id). `data-slice` stays **`"17"`** in `App.tsx` and `e2e/helpers.ts` `waitForGame`. Chrome 1366×768 and 1920×1080, `npm run dev` and `npm run preview`. Keep WORLD_POS landmarks (apartment door, dumpster, robot at `12*TILE+24` / `5*TILE+24`, shop `P`, parcel `R`, library `I`, inner `F`, newsroom `E`, festival `G`, workshop `Y`). Street letter `Y` stays `WORLD_POS.workshopDoor`. Workshop letters `u z m j k t d q a e w l h x f v J Z O V 1 2 3 4` unmoved. Row 8 stays `#O1wJfhZ.vx3l2V#` (`3` col 11, col 8 `.`). Row 6 stays `#k.q......t4...#` (`4` col 11, col 8 `.`). Door `d` col 8. **No new interior solid.** Do not occupy col 8. Do not weaken 01–17 acceptance criteria. Neighbor postpone + shop-enter reload + corrupt/denied storage in `save.spec.ts` still pass; do not redo them as the slice goal. RG03 other browsers stay untested (do not claim them).

## Out
- Slice 18 gameplay / sequel curriculum / a “next adventure” start.
- Claiming Chrome/Edge Windows, Firefox Windows, Safari macOS, or any RG03 matrix cell not actually run. Record Chrome/Linux only.
- Five-beginner usability pilot (RG04); publish/deploy; public certificate registry; remote name registry.
- Weakening any 01–17 acceptance criteria. Re-awarding 1.1–6.4. Adding a 35th evidence key. `JOURNAL_CAP` bump. New journal ids.
- New map, new portal, civic-lab, shifting WORLD_POS or workshop letters `u z m j k t d q a e w l h x f v J Z O V 1 2 3 4`, blocking door-column floor, occupying col 8, any new workshop solid.
- Persisting overlay `mode` (`path` / `ending` / `notice` / `inspect`) as the reload surface; changing hydrate away from `playing`.
- New npm PDF SDK; live `fetch`/`eval`; English `MCP`/`harness` in persistable GameState; putting `playerName` in download filenames, URL, title, or console.
- Treating neighbor postpone or shop-enter-after-help as the unfinished later-arc puzzle. Redoing corrupt/denied storage as the slice goal.
- Bumping `data-slice` off `"17"` (would break `waitForGame`).

## Constraints
- Implement in `app/`. **No new map and no new portal.** Reuse `path-desk` and the existing ending overlay. Keep `data-slice="17"` unless a documented blocker appears — there is none: `waitForGame` must keep requiring `data-slice="17"`. Prefer no `JOURNAL_CAP` bump; none is needed. Hydrate stays `mode: 'playing'` (slice 02 AC04: do not persist mid-bubble dialogue or overlay mode). Unfinished path progress is restored via persisted `pathQuest` flags; issued ending via persisted `endingState` + `passportQuest`. If a persist gap is found, fix `shouldPersist` / `toEnvelope` / `hydrateSave` for **existing** fields only — do not add overlay mode to the envelope. Authored buttons, not `eval`, not `fetch`, not `Date.now()` awards. Arabic RTL page; record/hour/version digits isolated LTR (`path-ltr`). WASD/arrows by `event.code`. Chrome-only evidence for this slice. Do not put English `MCP` / `harness` in persistable GameState, journal text, or HUD objectives. Never reuse `'paused'` / `TOGGLE_PAUSE` for path or ending. Never weaken 01–17 evidence predicates. Optional: export the AC01 default as a named constant (e.g. `CHECKPOINT_AFTER_HELP`) next to `OBJECTIVES` so `CheckpointNote` and tests share one string.

## Data / state impact
No new `MapId`. Evidence union **unchanged** (34 ids). No new interactable letter. WORLD_POS unchanged. FURNITURE unchanged. HINT_LABELS unchanged. `saveVersion` remains 1. `JOURNAL_CAP` remains 112. Journal id union unchanged. `EndingState` stays `'in_progress' | 'invited' | 'issued'`. `passportQuest` / `pathQuest` shapes unchanged. `toEnvelope` already writes `endingState: state.endingState`; keep that. Hydrate already accepts those three literals and treats unknown as `'in_progress'`. Overlay modes are **not** added to `SaveEnvelope`. `CONFIRM_NAME` still resets `passportQuest` and `endingState` to `'in_progress'`. Objective chain in `CheckpointNote` stays `issued` → `passportIssued`; else `invited` → `passportReady`; else `restored` → `restored`; else existing 16 chain; **only** the help-accepted default string changes as in AC01. Dummy `demo-slot-key` unchanged. SerializedTestState / SaveEnvelope unchanged unless a persist bug in existing fields requires a test-only helper. No new GameActions required.

## Tests
- T01 — Vitest `evidence/r1/state-tests.txt` for AC01–AC03: default checkpoint / `CHECKPOINT_AFTER_HELP` equals the AC01 sentence and ↛ `/قيد التطوير/`; `OBJECTIVES.cornerStore` unchanged; `OBJECTIVES.passportIssued` / `passportReady` / `restored` unchanged; WORLD_POS landmarks unchanged including robot `12*TILE+24` / `5*TILE+24`; workshop letters `u z m j k t d q a e w l h x f v J Z O V 1 2 3 4` unmoved; row 8 still `#O1wJfhZ.vx3l2V#` with `3` col 11 / col 8 `.`; row 6 still `#k.q......t4...#` with `4` col 11 / col 8 `.` / door `d` col 8; `JOURNAL_CAP === 112`; `EVIDENCE_IDS.length === 34`; `data-slice` still `"17"`. Unit: after `playToCrewDone` + inspect + refuse + reading-goal, `toEnvelope` / `validateSave` / `hydrateSave` restores those flags, `restored === false`, `6.4` undefined, `endingState === 'in_progress'`, hydrated `mode === 'playing'`. After a 17 issue fixture, hydrate `endingState === 'issued'`, `passportQuest.issued === true`, `storyObjective === OBJECTIVES.passportIssued`, journal `passport_issued` length 1, hydrate `mode === 'playing'`. Re-recording `passport_issued` / `source_verified` is a no-op. Lab/kiosk/agent/bridge/skill/approval/crew/path **success** states still `JSON.stringify(state)` ↛ `/MCP/` and ↛ `/harness/`. After 12 connect, `BRIDGE_EXPLAIN` / overlay note **does** still contain `MCP`. No `fetch(` and no `Date.now()` / `setInterval` awards in R1 edits.
- T02 — Playwright unfinished path: `playToCrewDone` → path-desk inspect + refuse + reading-goal → reload → reopen `path-desk` → progress restored → `completePathQuest` awards 6.4 once, no certificate. Screenshots `evidence/r1/unfinished.png`, `evidence/r1/resume-puzzle.png`. `test.setTimeout(300_000)` as in `path.spec.ts` / `passport.spec.ts`.
- T03 — Playwright issued reload: `playToPathDone` → thanks → second talk → confirm name → PNG or PDF issue → reload → `endingState === 'issued'`, overlay reopenable, no duplicate `passport_issued`, filenames `rafiq-passport.png` / `rafiq-passport.pdf`. Screenshot `evidence/r1/issued-reload.png`. Also Playwright help-accepted: `slice-checkpoint` has the AC01 sentence and ↛ `/قيد التطوير/`; screenshot `evidence/r1/checkpoint.png`. Keep `journey.spec.ts` passing; add the leftover-copy assertion there or in the R1 spec.
- T04 — Viewports 1366 and 1920; `evidence/r1/browser-checks.md`, `evidence/r1/overlays.png`; path-desk + certificate reopen readable at both sizes; version/record/hour digits `direction: ltr`; run against `npm run dev` and `npm run preview`. Record Chrome/OS/version; Firefox/Edge/Safari/Windows/macOS untested (RG03, not this slice).
- T05 — typecheck, lint, test, build; `evidence/r1/project-checks.txt`.
- T06 — Keep **01–17** e2e, including `save.spec.ts` (shop-enter after `help_accepted`, neighbor postpone, corrupt primary → backup, denied storage). `waitForGame` still requires `data-slice="17"`. Keep `path.spec.ts` `OBJECTIVES.restored` after 16 close, `certificate` count 0 after first robot talk, manager `/سُهرة القراءة نُشرت بعد سند NH-3301 وخطة محدودة وحزمة سياق ومهارة وموافقة بشرية، والروبوت صار جاهزاً تحت إشراف/`, companion `/الترميم يلغي الهلوسة|الامتحان الموقوت يكفي/` on first talk. Keep `passport.spec.ts` issue + download filenames. Keep `crew.spec.ts` / `approve.spec.ts` / `skill.spec.ts` / `bridge.spec.ts` / `agent.spec.ts` manager/companion regexes while their later flags are false. Do not retarget `waitForGame` or 01–17 evidence dirs. New evidence lives under **`evidence/r1/`** (lowercase, used consistently). Reviewer maps AC01–AC06 and verifies snapshot.

## Proof
Builder claims for d-20260909-072-impl-r1 (not independently accepted):
- Candidate (coordinator recomputed): `c3abdf149d10df285548c23525e96cd0769bb5cb1bf3481108d1a2a88d2cc44f` (336 files).
- Contract unchanged: `3611159b659f758cdea14412072b10733df4b9993ceeb894de461647b56f13a5`.
- Claimed checks: tsc/lint/build exit 0; vitest 145; Playwright 70/70 on preview :4600 and Vite :4601.
- Claimed artifacts: `evidence/r1/` checkpoint.png, unfinished.png, resume-puzzle.png, issued-reload.png, overlays.png, state-tests.txt, project-checks.txt, browser-checks.md.
- Claimed: leftover `/قيد التطوير/` gone; unfinished path-desk inspect+refuse+reading-goal survives reload; issued ending survives reload; `data-slice` stays `"17"`; `JOURNAL_CAP` 112.
Reviewer must re-run checks in an isolated copy and map AC01–AC06.

## Review
Plan approved. Implementation ready for independent review.
Plan approval: APPROVE_PLAN by reviewer bc-2547096e-ec7e-5e46-944e-d7755bf810c8 on dispatch d-20260909-071-plan-r1. Contract `3611159b659f758cdea14412072b10733df4b9993ceeb894de461647b56f13a5`. Snapshot `b680867179a7973832bbe4e06a99a3f77a8cde3ede5a507902af9eee879e07c0`. Blockers: none.
Implementation approval: none
Each result records dispatch ID, reviewer identity, verdict, contract identity, snapshot identity, evidence and criterion-specific blockers.

## Loop state
Execution mode / tool adapter: Cursor Cloud Agent coordinator with Task-spawned Builder and Reviewer subagents. Spawn = Task(generalPurpose). Send = Task resume. Wait = blocking Task completion. Stop = subagent completion; coordinator does not start a second writer in this checkout. Reviewer contexts are fresh and do not receive Builder reasoning. Mutating Reviewer checks, if needed, run on an isolated copy.
Coordinator: cloud agent bc-6380229a-c83f-493f-af1c-47e5f2b00c70 (https://cursor.com/agents/bc-6380229a-c83f-493f-af1c-47e5f2b00c70), role Coordinator, checkout /workspace on branch cursor/rafiq-continue-slices-0c70
Worker / role / phase: pending launch / Reviewer / implementation
Dispatch ID / launch state / input identity: d-20260909-073-implrev-r1 / pending launch / contract:3611159b659f758cdea14412072b10733df4b9993ceeb894de461647b56f13a5 candidate:c3abdf149d10df285548c23525e96cd0769bb5cb1bf3481108d1a2a88d2cc44f
Pending result / last consumed dispatch: none / d-20260909-072-impl-r1
Snapshot capture and recheck commands / coverage / exclusions: Capture = `python3 .loop/identity.py snapshot --label <label>` from repository root. Recheck = same command; compare `.loop/snapshots/<label>.digest` and the JSON `digest` field. Contract = `python3 .loop/identity.py contract`; identity is `.loop/contract/hashes.json` field `contract`. Combined = `python3 .loop/identity.py both --label <label>`.
Coverage: `app`, `evidence`, root `package.json`/`package-lock.json`/`pnpm-lock.yaml`/`yarn.lock`, `index.html`, `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `playwright.config.ts`, `vitest.config.ts`, `README.md`, `public`. Missing paths are skipped. Detect add/delete by regenerating the covered file list.
Exclusions: `.git`, `.loop`, `learnai-city-project-loop`, `node_modules`, `app/node_modules`, `app/dist`, `dist`, `coverage`, `test-results`, `playwright-report`, `.vite`, `app/.vite`. Protocol files are identified by contract hash, not candidate snapshot.
Baseline snapshot: shipped slice 17 `b680867179a7973832bbe4e06a99a3f77a8cde3ede5a507902af9eee879e07c0` (326 covered files)
Contract identity: `3611159b659f758cdea14412072b10733df4b9993ceeb894de461647b56f13a5` (`.loop/contract/hashes.json`)
Candidate snapshot: `c3abdf149d10df285548c23525e96cd0769bb5cb1bf3481108d1a2a88d2cc44f` (336 files)
Rejection count: 0
Consecutive no-progress repairs: 0
Open acceptance gaps / prior failing evidence: leftover checkpoint copy «بقية ألغاز المغامرة ما زالت قيد التطوير.»; no e2e save/reload of an unfinished later-arc puzzle; no e2e reload of an issued ending
Repair awaiting review: false
Review events:
- ev-001 / d-20260909-071-plan-r1 / plan / APPROVE_PLAN / contract:3611159b659f758cdea14412072b10733df4b9993ceeb894de461647b56f13a5 snapshot:b680867179a7973832bbe4e06a99a3f77a8cde3ede5a507902af9eee879e07c0 / gaps: none / identities matched / rejection count 0 / no-progress 0
Budget limit / consumed / measurement: Not configured; no execution budget was supplied.
Blocker / resume status / resume action / recheck condition / deadline: RG03 matrix and RG04 five-beginner pilot still unmet (not in this repair) / Running / independent implementation review R1 / APPROVE_IMPLEMENTATION then archive; Complete still needs RG03/RG04 or an explicit scope decision / none
Advance phase: none
Next slice ID / draft: none
Prior shipped receipt: slice 17 archive `slices/17-your-passport.md`

## Status
Ready for review

## Next
Independent implementation review of slice R1 under dispatch d-20260909-073-implrev-r1. Isolated copy only.
