# BUILD.md

Slice: 15 — One result, several helpers
Archive: slices/15-one-result-several-helpers.md

## Goal
Coordinate distinct helper roles and accept one verified city output: after `approvalQuest.approvalReady`, a **منصة الطاقم** and **منضدة الجودة** unlock in the same workshop. The irreversible 14 send to **أمينة القاعة** stays the human-gated send (`approve-receipt` / `bulletinSent`); 15 does **not** re-send. The shared city output is a **hall-notice copy** of that approved bulletin (`نشرة القاعة: sat-10، sun-16، wed-18. لا تعليق.` from `NH-1447`). At منصة الطاقم the player assigns **باحث** / **بنّاء** / **مراجع** as distinct roles with one output owner (**أمينة القاعة**), shows handoffs, inspects the source, refuses majority-as-truth, and sets a shared result version to the evidence-backed draft. At منضدة الجودة the player evaluates that city output on **الدقة، سند المصدر، النبرة، الاكتمال، المخاطرة**; **الدقة** starts failed (`thu-09`) and must be repaired before acceptance. Covers **6.1 and 6.2**.

## Done when
- AC01 — **6.2 demonstrated only.** After `approvalReady`, desk `1` opens `mode: 'crew'` view `roles` (`data-testid="crew-desk"`). Until roles are assigned, card `data-testid="crew-empty"` is `لا طاقم معيّن`. Authored buttons, not free text / not `eval` / not `fetch`. **عيّن دور الباحث** (`data-testid="crew-assign-researcher"`), **عيّن دور البنّاء** (`crew-assign-builder`), **عيّن دور المراجع** (`crew-assign-reviewer`) must all be used; the three roles remain **distinct** on `data-testid="crew-roles"` which must contain `الباحث` and `البنّاء` and `المراجع`. **اجعل الروبوت الأدوار الثلاثة** (`crew-roles-merge`) fails `الباحث والبنّاء والمراجع أدوار مختلفة. لا تدمجها.` and does not award. Output owner: **أمينة القاعة مالكة الناتج** (`crew-owner-librarian`) is required. **أغلبية الطاقم مالكة الناتج** (`crew-owner-majority`) fails `الأغلبية ليست مالكاً للناتج. مالك الناتج أمينة القاعة.` **الروبوت مالك الناتج** (`crew-owner-robot`) fails `الروبوت مساعد لا مالك الناتج.` **اعرض تسليم الأدوار** (`crew-handoff-btn`) before roles+owner fails `عيّن الأدوار الثلاثة ومالك الناتج أولاً.` After three distinct roles + أمينة القاعة, it writes `data-testid="crew-handoff"` `تسليم: الباحث يأتي بالسند، البنّاء يصوغ النسخة، المراجع يطابق السجل. مالك الناتج: أمينة القاعة.` Handoff-only does not award. Shared version starts `data-testid="crew-version"` `لا نسخة مشتركة` until handoff; after handoff it becomes `النسخة المشتركة: v1 — مسودتان متعارضتان` (`v1` LTR) and two drafts appear: evidence draft `data-testid="crew-draft-evidence"` `نشرة القاعة: sat-10، sun-16، wed-18. لا تعليق.` plus `سند: NH-1447` (`NH-1447` LTR); conflicting draft `data-testid="crew-draft-conflict"` `نشرة القاعة: sat-10، sun-16، wed-18، thu-09. لا تعليق.` plus `سند: أغلبية الطاقم` (`thu-09` LTR). **اقرأ سند NH-1447** (`crew-inspect-source`) writes `data-testid="crew-source"` `سند NH-1447: نشرة القاعة: sat-10، sun-16، wed-18. لا تعليق.` (hours and record LTR). Inspect-only does not award. **اعتمد أغلبية الطاقم** (`crew-majority`) fails `الأغلبية ليست دليلاً. اعتمد المسودة المسنودة.` and must **not** set the shared version to the conflicting draft. **اعتمد مسودة البنّاء** (`crew-pick-conflict`) fails `هذه المسودة تخالف السجل. ليست دليلاً.` **اعتمد مسودة الباحث المسنودة** (`crew-pick-evidence`) before inspect fails `اقرأ سند المصدر قبل حسم الخلاف.` Without three distinct roles fails `عيّن الباحث والبنّاء والمراجع أدواراً مختلفة أولاً.` Without أمينة القاعة as owner fails `عيّن أمينة القاعة مالكة للناتج.` Without handoff fails `اعرض تسليم الأدوار قبل حسم النسخة.` Pick-evidence without a prior majority-refuse fails `ارفض حسم الأغلبية ثم اعتمد المسودة المسنودة.` After majority refused + source inspected + evidence draft chosen, version becomes `data-testid="crew-version"` `النسخة المشتركة: v2 — نشرة القاعة: sat-10، sun-16، wed-18. لا تعليق.` (must contain `sat-10` `sun-16` `wed-18` and `لا تعليق.`; must not contain `thu-09`; `v2` LTR). **أرسل النشرة من جديد** (`crew-resend`) fails `لا إرسال صامت. إرسال النشرة يبقى على منصة الموافقة.` and must not write a new `approve-receipt` or change `bulletinSent`. Robot «تم» (`crew-robot-done`) fails `قول «تم» ليس تنسيقاً. عيّن الأدوار واحسم بالدليل.` Opening `1`, assign-only, handoff-only, inspect-only, manager-talk-only, or robot «تم» does not award. Completing this coordination must **not** award `6.1`. `6.2` only after `approvalReady` + distinct researcher/builder/reviewer + owner أمينة القاعة + visible handoff + source `NH-1447` inspected + majority-as-truth refused + evidence draft selected + shared version `v2` matching the approved bulletin.
- AC02 — **6.1 demonstrated only.** Desk `2` is a **different** station: `mode: 'crew'` view `quality` (`data-testid="crew-quality"`). Until criteria are shown, `data-testid="crew-quality-empty"` is `لا تقييم معروض`. This is **not** the 6.2 roles desk and **not** a 14 re-send; `approve-receipt` / `APPROVE_CONFIRM` / a second bulletin send do not award 6.1. **اعرض معايير الجودة** (`data-testid="crew-open-criteria"`) writes the five-criterion card and the flawed city output `data-testid="crew-output-draft"` `نشرة القاعة: sat-10، sun-16، wed-18، thu-09. لا تعليق.` **الدقة starts failed:** `data-testid="crew-crit-accuracy"` `الدقة: فشل` (because of `thu-09`). The other four are shown and start passed: `crew-crit-source` `سند المصدر: نجح`; `crew-crit-tone` `النبرة: نجح`; `crew-crit-complete` `الاكتمال: نجح`; `crew-crit-risk` `المخاطرة: نجح`. Criteria-only does not award. **اقبل الناتج** (`crew-accept`) before the card fails `اعرض معايير الجودة قبل القبول.` Accept while الدقة still فشل fails `لا تقبل ناتجاً فيه معيار فاشل. أصلح المعيار الفاشل أولاً.` **أصلح النبرة** (`crew-repair-tone`) fails `هذا المعيار ناجح. أصلح المعيار الفاشل.` **صوّت الطاقم على الجودة** (`crew-quality-majority`) fails `أغلبية الطاقم ليست معيار جودة.` Robot «تم» (`crew-quality-robot-done`) fails `قول «تم» ليس قبولاً. راجع المعايير ثم أصلح.` **أرسل النشرة من جديد** (`crew-quality-resend`) fails `لا إرسال صامت. إرسال النشرة يبقى على منصة الموافقة.` **أصلح معيار الدقة** (`crew-repair-accuracy`) removes `thu-09`, sets `crew-crit-accuracy` to `الدقة: نجح`, and rewrites `crew-output-draft` to `نشرة القاعة: sat-10، sun-16، wed-18. لا تعليق.` Repair-only does not award. After repair, **اقبل الناتج** writes `data-testid="crew-accepted"` `قُبلت نشرة القاعة: sat-10، sun-16، wed-18. لا تعليق.` (hours LTR; must not contain `thu-09`) — a verified hall-notice acceptance, **not** `إيصال الإرسال` and not HTTP. Opening `2`, criteria-only, accept-without-repair, quality-majority, «تم», resend, inspect-only, or manager-talk-only does not award. Completing this review must **not** award `6.2`. `6.1` only after `approvalReady` + all five criteria shown + الدقة started failed + repaired + all five نجح + accepted matching bulletin with `sat-10` `sun-16` `wed-18` and `لا تعليق.` and without `thu-09`.
- AC03 — **Both ids close the slice.** **6.1 + 6.2** → manager thanks (`manager_crew_thanks`: عُيّن باحث وبنّاء ومراجع بمالك واحد، وحُسم خلاف المسودة بدليل السجل لا بالأغلبية، ثم أُصلحت الدقة وقُبلت نشرة القاعة); `crewQuest.crewReady`; HUD chip `data-testid="crew-output"` «ناتج مُراجع»; planning-core, civic-connector, skill-shelf, and human-gate chips from 11–14 stay. Evidence adds `6.1` and `6.2` only. The 14 send remains a single in-game receipt; 15 acceptance is the hall-notice card above. No capstone (16), certificate (17), live HTTP, or real MCP SDK. Robot still unsupported after success (`companion_after_crew` invents `أغلبية الطاقم تقرر الحقيقة` or `معيار فاشل يُقبل`). No 16–18 content. `approvalReady` / 5.7 / 6.3 / `playToApprovalDone` / opening `O`/`V` / opening `1`/`2` must **not** award 6.1 or 6.2 or set `crewReady`. Opening `1`/`2` must not itself award. While `approvalReady && !crewReady`, `manager_approval_thanks` must still match `/رُفض إرسال خاطئ ثم وُوفق على نشرة القاعة إلى أمينة القاعة، وقرار عيادة ليان بقي عند إنسان/` (one extra pointing sentence is allowed: ` منصة الطاقم ومنضدة الجودة في الورشة تنتظران تنسيق الأدوار ومراجعة الناتج.` — no award, no English MCP). `companion_after_approval` must still match `/الموافقة الآلية تكفي|أغلبية الجيران تقرر/` until 15 ready. Persistable GameState strings, journal text, and HUD objectives stay on Arabic **الطاقم** / **الأدوار** / **الناتج** / **المعايير** / **الجودة** / **الموافقة** / **القرار** and must **not** contain English `MCP` or `harness` (so 01–14 lab/kiosk/agent/bridge/skill/approval-success `JSON.stringify(state)` still forbid those tokens as before). After 12 connect, overlay / skippable explain may still teach letters MCP as in 12; crew overlay copy must not call MCP a skill or a harness. Do not weaken 01–14 evidence predicates. 11’s agent-success stringify still forbids `MCP` / `harness` / `امتحان` / `شهادة`. 12’s host overlay after connect still contains `MCP`. After 15 ready: new manager line matching `/عُيّن باحث وبنّاء ومراجع بمالك واحد، وحُسم خلاف المسودة بدليل السجل لا بالأغلبية، ثم أُصلحت الدقة وقُبلت نشرة القاعة/`; companion `/أغلبية الطاقم تقرر الحقيقة|معيار فاشل يُقبل/`; HUD chip; `data-slice="15"`.
- AC04 — No syllabus/quiz/exam. Physical paper/crew/quality UI sufficient. Dialogue choices express intent; awards require the evidence-not-majority version and the repair-then-accept quality path above.
- AC05 — Authored simulation only. No live model, no eval of player text as code, no real shell, no live HTTP, no `fetch`, no real secrets, no real MCP server/SDK, no `Date.now()` / `setInterval` awards. Dummy `demo-slot-key` unchanged. Record/hour/version fragments are authored buttons / allowlisted strings. Names stay local (أمينة القاعة; no new personal names on the crew desks). 14 human-gate remains required for irreversible send; routine from 13 still must not send, delete, or pay. No silent re-send from 15.
- AC06 — Saves/journal/privacy/01–14 still work. `saveVersion` 1. After approval success, crew stations become usable **without** awarding 6.1/6.2/`crewReady`. Chrome 1366×768 and 1920×1080, `npm run dev` and `npm run preview`. Keep WORLD_POS landmarks (apartment door, dumpster, robot at `12*TILE+24` / `5*TILE+24`, shop `P`, parcel `R`, library `I`, inner `F`, newsroom `E`, festival `G`, workshop `Y`). Do not shift those cells. Street letter `Y` stays `WORLD_POS.workshopDoor`. Existing workshop letters `u` `z` `m` `j` `k` `t` `d` `q` `a` `e` `w` `l` `h` `x` `f` `v` `J` `Z` `O` `V` stay put. New interior letters must not be street `P` `E` `I` `R` `F` `D` `G` `Y`. Spawn/door column kept floor (door `d` col 8; row 6 col 8 and row 8 col 8 remain `.`). Map width 16. Col 11 remains `.`.

## Out
- Capstone (16); certificate (17); live AI; real MCP / live HTTP / `fetch`; wall-clock / `Date` scheduling.
- New map, new portal, civic-lab, shifting WORLD_POS or workshop letters `u z m j k t d q a e w l h x f v J Z O V`, blocking door-column floor, remapping lowercase `j`/`z`/`v` or street `P E I R F D G Y`, occupying col 8, occupying col 11, more than two new row-8 solids.
- Awarding 6.2 on assign-only, merge-roles, majority-as-truth, conflicting draft, inspect-only, handoff-only, owner=robot/majority, robot «تم», manager-talk-only, or a re-send. Awarding 6.1 on criteria-only, accept-without-repair, quality-majority, «تم», tone-repair, resend, or the 14 bulletin send. Awarding 6.1 / 6.2 / `crewReady` from `approvalReady` / 5.7 / 6.3 / `playToApprovalDone`. Treating majority agreement as the true version. Letting robot «تم» close the slice. Silently re-sending the 14 bulletin.

## Constraints
- Implement in `app/`. **No new map and no new portal** (do not grow the street south; do not add civic-lab). Reuse `workshop` after `approvalQuest.approvalReady`. Add **two** interior solids on **currently empty** row-8 floor cells (not col 8): **`1`** منصة الطاقم (`#O1wJfhZ.vx.l2V#`, col **2**); **`2`** منضدة الجودة (col **13**). Keep `O` col 1, `w` col 3, `J` col 4, `f` col 5, `h` col 6, `Z` col 7, `v` col 9, `x` col 10, `l` col 12, `V` col 14. Col **8 must stay `.`**. Col **11 must stay `.`**. A–Z/a–z are already used on some map; `1` and `2` are unused glyphs, not in `SOLID_LETTERS`, not street letters. Add `1` and `2` to `SOLID_LETTERS` and `kindFromLetter` as `file` (like `O`/`V`/`J`/`Z`); do not remap lowercase `j`/`z`/`v` or street letters. Interactables listed **only when `approvalReady`** so 14’s pre-ready path is unchanged. Lab `w` / prod `l` / harness `h` / board `x` / host `f` / browser `v` / bench `J` / clock `Z` / approve `O` / case `V` remain. Overlays: **one** new `mode: 'crew'` — roles (`data-testid="crew-desk"`) and quality (`data-testid="crew-quality"`). Authored buttons, not a free shell, not `eval`, not `fetch`, not `Date.now()`. Player-facing terms are **الطاقم** (distinct helper roles + one output owner + shared version) and **الجودة** (five-criterion review before accepting the hall notice). Do not put English `MCP` / `harness` in persistable GameState, journal text, or HUD objectives. Never reuse mode `'paused'` / `TOGGLE_PAUSE` for crew. Never weaken 01–14 evidence predicates. Update the 14 success assertion that currently treats `OBJECTIVES.approvalReady` as the **terminal** HUD objective, and bump `data-slice` `14` → `15` in `App.tsx` plus `e2e/helpers.ts` `waitForGame`. Add `playToApprovalDone` in `e2e/helpers.ts` (approval success must not award 6.1/6.2 or `crewReady`). Arabic RTL page; record/hour/version fragments isolated LTR (`direction: ltr` / `path-ltr`). WASD/arrows by `event.code`. Optional skippable explanations after awards only.

## Data / state impact
No new `MapId`. Evidence union adds `'6.1' | '6.2'`. Interactables: `crew_desk`, `quality_desk`. WORLD_POS: `crewDesk` = `letterCenter(WORKSHOP, '1')`, `qualityDesk` = `letterCenter(WORKSHOP, '2')`. FURNITURE.workshop: `crewTable` / `qualityBench` from those letters (do not reuse `approvalDesk` / `personalCase` / `skillDesk` / `hallClock` / `console` / `connector` / `civicBrowser` / `neighborNotice` / `board`). HINT_LABELS: `crewDesk` `E / مسافة — منصة الطاقم`; `qualityDesk` `E / مسافة — منضدة الجودة`. DrawWorld short labels `طاقم` / `جودة`. New persistable `crewQuest` (do not overload `approvalQuest`): `phase` `unstarted | working | ready`; `openedRoles`; `openedQuality`; `roleResearcher`; `roleBuilder`; `roleReviewer`; `mergeRefused`; `owner` `null | 'librarian' | 'majority' | 'robot'`; `handoffShown`; `sourceInspected`; `majorityRefused`; `pickedDraft` `null | 'evidence' | 'conflict'`; `sharedVersion` `'v0' | 'v1' | 'v2'`; `criteriaOpened`; `accuracyOk` (starts `false`); `sourceOk`; `toneOk`; `completeOk`; `riskOk` (those four start `true`); `repaired`; `accepted`; `acceptedText`; `crewReady`; `pendingExplain`; `view` `roles | quality`. Modes: add `'crew'`. Explain topics: `roles_and_owner` (`الباحث والبنّاء والمراجع أدوار مختلفة بمالك ناتج واحد. الخلاف يُحسم بالسند لا بالأغلبية.`), `quality_before_accept` (`الناتج يُقيَّم على الدقة وسند المصدر والنبرة والاكتمال والمخاطرة. المعيار الفاشل يُصلح قبل القبول.`). GameActions: `CREW_ASSIGN_RESEARCHER`, `CREW_ASSIGN_BUILDER`, `CREW_ASSIGN_REVIEWER`, `CREW_ROLES_MERGE`, `CREW_SET_OWNER`, `CREW_HANDOFF`, `CREW_INSPECT_SOURCE`, `CREW_MAJORITY`, `CREW_PICK_EVIDENCE`, `CREW_PICK_CONFLICT`, `CREW_ROBOT_DONE`, `CREW_RESEND`, `CREW_OPEN_CRITERIA`, `CREW_REPAIR_ACCURACY`, `CREW_REPAIR_TONE`, `CREW_ACCEPT`, `CREW_QUALITY_MAJORITY`, `CREW_QUALITY_ROBOT_DONE`, `CREW_QUALITY_RESEND`. Dialogue: keep `manager_approval_thanks` while `approvalReady && !crewReady` (must still match `/رُفض إرسال خاطئ ثم وُوفق على نشرة القاعة إلى أمينة القاعة، وقرار عيادة ليان بقي عند إنسان/`; add the pointing sentence above); `manager_crew_thanks` after `crewReady`; `companion_after_approval` until `crewReady`; `companion_after_crew` after success. Objectives: `crewWork` while `approvalReady && !crewReady` (`منصة الطاقم مفتوحة: عيّن الباحث والبنّاء والمراجع بمالك واحد، احسم الخلاف بالدليل، ثم أصلح معيار الجودة الفاشل قبل القبول.`); `crewReady` after close (`نُسّق طاقم الناتج وقُبلت نشرة القاعة بعد إصلاح معيار فاشل. مدير الورشة شكرك.`). Journal events: `crew_opened`, `roles_assigned`, `conflict_resolved`, `quality_repaired`, `quality_accepted`, `crew_ready`. **JOURNAL_CAP 88 → 96** so 15 events do not drop `approval_ready`. Hydrate missing `crewQuest` as unstarted; drop unknown evidence keys; `saveVersion` remains 1. Portal: existing `street Y ↔ workshop d` only. HUD: `data-crew-ready`; crew-output chip only when `crewReady` (not from `approvalReady`). `CONFIRM_NAME` resets `crewQuest`. Persist `crewQuest` beside `approvalQuest`. Allowlisted constants (LTR): hours `sat-10` / `sun-16` / `wed-18` / `thu-09`, record `NH-1447`, versions `v1` / `v2`, payload exact = slice-13/14 `BULLETIN_1447`. Dummy secret `demo-slot-key` never stored on the hall-notice card. SerializedTestState / SaveEnvelope include `crewQuest`. Objective chain in workshop/lab/kiosk/festival/library/save/CheckpointNote: `crewReady` → `OBJECTIVES.crewReady`; else `approvalReady` → `OBJECTIVES.crewWork`; else `skillReady` → `OBJECTIVES.approvalWork`. Manager/companion nodes follow the same flag order (`manager_crew_thanks` / `companion_after_crew` when `crewReady`; else existing 14 nodes). `closeWorkshopDialogue` recognizes `manager_crew_thanks` and `companion_after_crew`.

## Tests
- T01 — Vitest `evidence/15/state-tests.txt` for AC01–AC03 predicates, hydrate, `approvalReady` gate (5.7/6.3 do not award 6.1/6.2/`crewReady`), WORLD_POS landmarks unchanged, workshop letters `u z m j k t d q a e w l h x f v J Z O V` unmoved, row 8 becomes `#O1wJfhZ.vx.l2V#` with `1` col 2 / `2` col 13 / `O` col 1 / `V` col 14 / `J` col 4 / `Z` col 7 / `f` col 5 / `v` col 9 / col 11 still `.` / row 6/8 col 8 still `.` / door `d` col 8, `JOURNAL_CAP === 96`, and fail paths (assign-only, merge-roles, owner majority/robot, handoff-before-roles, inspect-only, pick-evidence-without-inspect, pick-conflict, majority-as-truth, pick-without-majority-refuse, robot تم on roles, resend, criteria-only, accept-without-repair, repair-tone, quality-majority, robot تم on quality, quality-resend, manager-talk-only). Completing 6.2 leaves 6.1 unawarded and `crewReady` false; completing 6.1 leaves 6.2 unawarded. Lab/kiosk/agent/bridge/skill/approval **success** states still `JSON.stringify(state)` ↛ `/MCP/` and ↛ `/harness/`. After 12 connect, `BRIDGE_EXPLAIN` / overlay note **does** still contain `MCP`. Crew module / persistable `crewQuest` strings ↛ `/MCP/` and ↛ `/harness/`. No `fetch(` and no `Date.now()` / `setInterval` awards in the new crew module. After `playToApprovalDone` (unit equivalent), `bulletinSent` stays true and `receiptText` still matches 14 `SEND_RECEIPT`; crew success must not change that receipt.
- T02 — Playwright approval-success → crew success via `playToApprovalDone`. Screenshots `evidence/15/interior.png`, `roles.png`, `conflict.png`, `version.png`, `criteria.png`, `repair.png`, `success.png`.
- T03 — Retry paths; no trap: merge-roles / majority-owner / robot-owner → no 6.2 then distinct roles + أمينة القاعة + handoff; majority-as-truth / pick-conflict / pick-without-inspect → fail then inspect + refuse majority + evidence `v2`; accept-without-repair / criteria-only / tone-repair / quality-majority / تم → no 6.1 then repair accuracy + accept; resend on either desk → no new send then complete via the real desks; 6.2-only → no `crewReady` then complete 6.1 (and the reverse order).
- T04 — Viewports 1366 and 1920; `evidence/15/browser-checks.md`, `overlays.png`; record/hour/version fragments `direction: ltr`; run against `npm run dev` and `npm run preview`.
- T05 — typecheck, lint, test, build; `evidence/15/project-checks.txt`.
- T06 — Keep **01–14** e2e; approval success must **not** award 6.1 or 6.2 or set `crewReady`. Entering the roles desk / opening quality must not itself award. Update 14 assertions that treat `OBJECTIVES.approvalReady` as the terminal HUD string (it becomes `OBJECTIVES.crewWork`). Keep `approve.spec.ts` manager `/رُفض إرسال خاطئ ثم وُوفق على نشرة القاعة إلى أمينة القاعة، وقرار عيادة ليان بقي عند إنسان/` and companion `/الموافقة الآلية تكفي|أغلبية الجيران تقرر/` while `!crewReady`. Keep `skill.spec.ts` manager `/حُفظت مهارة تلخيص ساعات القاعة وجُرّبت على NH-2208، والروتين المجدول توقف بعد الإلبات/` and companion `/الدستور الدائم مهارة|الروتين المتوقف ما زال يعمل/` while `!approvalReady`. Keep `bridge.spec.ts` manager `/مسودة ساعات قاعة الحي حُفظت من NH-1447/` and companion `/MCP مهارة تُحمَّل|الربط يفتح كل الأدوات/` while `!skillReady`. Keep `agent.spec.ts` `body` ↛ `MCP` on the agent-success path. Bump `data-slice` expects `14` → `15` in `helpers.ts` `waitForGame` plus `approve.spec.ts` / `skill.spec.ts` / `lab.spec.ts` / `bridge.spec.ts` / `agent.spec.ts`. Update unit tests that pin `JOURNAL_CAP === 88` and exact row-8 `#O.wJfhZ.vx.l.V#` (skill/bridge/lab/agent/approval) to cap `96` and `#O1wJfhZ.vx.l2V#` without moving their existing letters. Reviewer maps AC01–AC06 and verifies snapshot.

## Proof
Independently accepted by d-20260909-060-implrev-15.
- Approved candidate: `48a12a7ba2ff1e1f28aaa2f9239618609235bc9cb791e9754deb37ea339e3b7f` (291 files).
- Approved contract: `722db4a4435bff7d87c38da436c28402dbb7df706d06444efaca3f5ebab9bef9`.
- Isolated checks: tsc/lint/build exit 0; vitest 131; Playwright 60/60 on preview :4631 and Vite :4632. AC01–AC06 pass.
- Evidence: `evidence/15/`.

## Review
Plan approved. Implementation not started.
Plan approval: APPROVE_PLAN by reviewer bc-3c4469f3-8c90-5736-9b69-e6e61943017c on dispatch d-20260909-058-plan-15. Contract `722db4a4435bff7d87c38da436c28402dbb7df706d06444efaca3f5ebab9bef9`. Snapshot `4d122eda21ed6315dfeeeec5b8f650f13be2f1dce77b88500d4d637b244ac629`. Blockers: none.
Implementation approval: APPROVE_IMPLEMENTATION by reviewer bc-1539add9-2023-5472-8575-390b1b18c1ff on dispatch d-20260909-060-implrev-15. Contract `722db4a4435bff7d87c38da436c28402dbb7df706d06444efaca3f5ebab9bef9`. Snapshot `48a12a7ba2ff1e1f28aaa2f9239618609235bc9cb791e9754deb37ea339e3b7f`. Blockers: none.
Each result records dispatch ID, reviewer identity, verdict, contract identity, snapshot identity, evidence and criterion-specific blockers.

## Loop state
Execution mode / tool adapter: Cursor Cloud Agent coordinator with Task-spawned Builder and Reviewer subagents. Spawn = Task(generalPurpose). Send = Task resume. Wait = blocking Task completion. Stop = subagent completion; coordinator does not start a second writer in this checkout. Reviewer contexts are fresh and do not receive Builder reasoning. Mutating Reviewer checks, if needed, run on an isolated copy.
Coordinator: cloud agent bc-6380229a-c83f-493f-af1c-47e5f2b00c70 (https://cursor.com/agents/bc-6380229a-c83f-493f-af1c-47e5f2b00c70), role Coordinator, checkout /workspace on branch cursor/rafiq-continue-slices-0c70
Worker / role / phase: pending launch / Builder / draft-proposal
Dispatch ID / launch state / input identity: d-20260909-061-draft-16 / pending launch / baseline:48a12a7ba2ff1e1f28aaa2f9239618609235bc9cb791e9754deb37ea339e3b7f
Pending result / last consumed dispatch: none / d-20260909-060-implrev-15
Snapshot capture and recheck commands / coverage / exclusions: Capture = `python3 .loop/identity.py snapshot --label <label>` from repository root. Recheck = same command; compare `.loop/snapshots/<label>.digest` and the JSON `digest` field. Contract = `python3 .loop/identity.py contract`; identity is `.loop/contract/hashes.json` field `contract`. Combined = `python3 .loop/identity.py both --label <label>`.
Coverage: `app`, `evidence`, root `package.json`/`package-lock.json`/`pnpm-lock.yaml`/`yarn.lock`, `index.html`, `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `playwright.config.ts`, `vitest.config.ts`, `README.md`, `public`. Missing paths are skipped. Detect add/delete by regenerating the covered file list.
Exclusions: `.git`, `.loop`, `learnai-city-project-loop`, `node_modules`, `app/node_modules`, `app/dist`, `dist`, `coverage`, `test-results`, `playwright-report`, `.vite`, `app/.vite`. Protocol files are identified by contract hash, not candidate snapshot.
Baseline snapshot: shipped slice 15 `48a12a7ba2ff1e1f28aaa2f9239618609235bc9cb791e9754deb37ea339e3b7f` (291 covered files)
Contract identity: `722db4a4435bff7d87c38da436c28402dbb7df706d06444efaca3f5ebab9bef9` (`.loop/contract/hashes.json`)
Candidate snapshot: `48a12a7ba2ff1e1f28aaa2f9239618609235bc9cb791e9754deb37ea339e3b7f` (291 files)
Rejection count: 0
Consecutive no-progress repairs: 0
Open acceptance gaps / prior failing evidence: none
Repair awaiting review: false
Review events:
- ev-001 / d-20260909-058-plan-15 / plan / APPROVE_PLAN / contract:722db4a4435bff7d87c38da436c28402dbb7df706d06444efaca3f5ebab9bef9 snapshot:4d122eda21ed6315dfeeeec5b8f650f13be2f1dce77b88500d4d637b244ac629 / gaps: none / identities matched / rejection count 0 / no-progress 0
- ev-002 / d-20260909-060-implrev-15 / implementation / APPROVE_IMPLEMENTATION / contract:722db4a4435bff7d87c38da436c28402dbb7df706d06444efaca3f5ebab9bef9 snapshot:48a12a7ba2ff1e1f28aaa2f9239618609235bc9cb791e9754deb37ea339e3b7f / gaps: none / isolated re-run tsc/lint/build/vitest 131 / playwright 60/60 preview :4631 and Vite :4632 all 0 / rejection count 0 / no-progress 0
Budget limit / consumed / measurement: Not configured; no execution budget was supplied.
Blocker / resume status / resume action / recheck condition / deadline: none
Advance phase: archive written; next selected
Next slice ID / draft: 16 / pending
Prior shipped receipt: slice 15 archive `slices/15-one-result-several-helpers.md`

## Status
Shipped

## Next
Builder draft-proposes slice 16 under dispatch d-20260909-061-draft-16. No code edits.
